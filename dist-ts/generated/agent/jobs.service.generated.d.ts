/**
 * JobsServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in JobsService.
 *
 * Generated at: 2026-08-05T22:37:04.000Z
 */
import { HttpClient } from '../http-client.js';
import type { AgentGetJobResponse, AgentDeleteJobResponse, AgentGetJobResultResponse } from '../types.js';
export declare abstract class JobsServiceBase {
    private defaultUrlTemplateVariables?;
    protected readonly http: HttpClient;
    protected readonly _kitNamespace: string | undefined;
    private readonly urlTemplatePattern;
    constructor(http: HttpClient, namespace?: string, defaultUrlTemplateVariables?: Record<string, string | number> | undefined, urlTemplatePattern?: string);
    /**
     * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
     */
    private _wrapHttpClient;
    /**
     * Build URL with template variables or fallback to baseURL
     *
     * @param path - API endpoint path
     * @param variables - Method-level template variables to override defaults
     * @returns Full URL (template mode) or path only (baseURL mode)
     */
    protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string;
    /**
     * Read nested values from objects using dotted paths.
     * Example: "data.items" or "data.pagination.total"
     */
    protected getPathValue(input: unknown, path: string): unknown;
    /**
     * Get an async job&#x27;s status.
     *
     * Returns the status of an async job (dispatch / workflow run / long tool call). run_id is null during the brief dispatch window for workflow runs.
     */
    getJob(id: string, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGetJobResponse>;
    /**
     * Cancel a pending/running job, or delete a finished record.
     *
     * Cancels a PENDING/RUNNING async job, or deletes a TERMINAL (succeeded/failed/canceled) job's immutable historical record — the kit /jobs convention. A pending/running job transitions to `canceled` and its work is stopped at the source: a sessionless run (headless / long tool call) has its bounded context cancelled (the daemon honors the cancelled context); a session dispatch / workflow turn is stopped via session.cancel (the active turn is cancelled, the session and its background tasks are spared). A terminal job's record is removed. Returns 200 {status:ok, canceled:true} on a cancel and {status:ok, deleted:true} on a terminal-record delete; 404 when the job id is unknown. The cancel is authoritative — a late terminator never flips a canceled job back to succeeded/failed. EXCEPTION: an in-flight answer:assist job is NOT independently cancellable (the daemon helper is bound to the question lifecycle with no per-assist cancel on the wire) — DELETE returns 409 job_not_cancellable rather than falsely reporting a cancel; it completes on its own or is cancelled by answering the question / cancelling the session turn.
     */
    deleteJob(id: string, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentDeleteJobResponse>;
    /**
     * Get an async job&#x27;s result.
     *
     * Returns the result of a completed job, or the running status (dispatch jobs are observed on the session stream — the result is terminal status only).
     */
    getJobResult(id: string, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGetJobResultResponse>;
}
