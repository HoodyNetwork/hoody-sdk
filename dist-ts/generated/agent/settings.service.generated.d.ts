/**
 * SettingsServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in SettingsService.
 *
 * Generated at: 2026-08-06T22:43:11.000Z
 */
import { HttpClient } from '../http-client.js';
import type { AgentGetACPStatusResponse, AgentSetACPEnabledRequest, AgentSetACPEnabledResponse, AgentSetACPAgentModelRequest, AgentSetACPAgentModelResponse, AgentSetACPSecretRequest, AgentSetACPSecretResponse, AgentGetSettingsResponse, AgentPatchSettingsRequest, AgentPatchSettingsResponse, AgentListFusionResponse, AgentUpsertFusionRequest, AgentUpsertFusionResponse, AgentDeleteFusionResponse } from '../types.js';
export declare abstract class SettingsServiceBase {
    private defaultUrlTemplateVariables?;
    protected readonly http: HttpClient;
    protected readonly _kitNamespace: string | undefined;
    private readonly urlTemplatePattern;
    constructor(http: HttpClient, namespace?: string, defaultUrlTemplateVariables?: Record<string, string | number> | undefined, urlTemplatePattern?: string);
    /**
     * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
     */
    private _wrapHttpClient;
    /**
     * Build URL with template variables or fallback to baseURL
     *
     * @param path - API endpoint path
     * @param variables - Method-level template variables to override defaults
     * @returns Full URL (template mode) or path only (baseURL mode)
     */
    protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string;
    /**
     * Read nested values from objects using dotted paths.
     * Example: "data.items" or "data.pagination.total"
     */
    protected getPathValue(input: unknown, path: string): unknown;
    /**
     * Get BYOA ACP backend status.
     *
     * Reports the BYOA delegated-session backend availability (claude) — enabled flag, on-PATH status, trust posture (acp.status). A fixed status object, NOT a paginated collection. SCOPE: the report is resolved through the layered settings the delegated session would use, so X-Hoody-Config-Dir (override) and X-Hoody-Cwd (project layer) are folded. For REMOTE BYOA, a bound X-Hoody-Container switches the report to ON-CONTAINER detection/login probing, and X-Hoody-Realm scopes that probe (threaded as realm_selector). A realm header WITHOUT a container returns 400 realm_scope_unsupported (the local report has no realm dimension). The per-agent secret value WRITE is PUT /acp/agents/{agent}/secrets/{key} (setACPSecret).
     */
    getACPStatus(options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGetACPStatusResponse>;
    /**
     * Enable or disable a BYOA ACP backend.
     *
     * Arms (or disarms) a BYOA ACP backend for delegated sessions (acp.set_enabled). A delegated session — POST /sessions with backend:"acp" — is refused by the daemon while its backend is disabled, so this is the prerequisite for the whole delegated-session capability on a host configured over HTTP. Writes tools.acp_agents.<agent>.enabled in settings.json. 404 unknown_agent for anything that is not a known BYOA backend (claude).
     */
    setACPEnabled(agent: string, data?: AgentSetACPEnabledRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentSetACPEnabledResponse>;
    /**
     * Set a BYOA backend&#x27;s default model and effort.
     *
     * Sets the default model (and reasoning effort) a BYOA ACP backend runs on (acp.set_agent_model). These are the values the daemon passes to the backend at connect; a delegated session started without an explicit model inherits them. An empty value clears the pin and returns the backend to its own default. 404 unknown_agent for anything that is not a known BYOA backend (claude).
     */
    setACPAgentModel(agent: string, data?: AgentSetACPAgentModelRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentSetACPAgentModelResponse>;
    /**
     * Store an ACP per-agent secret value.
     *
     * Stores (or clears) one per-backend env VALUE for a BYOA ACP agent (acp.set_secret) in the dedicated 0600 ~/.hoody/acp-secrets.env store under acp/<agent>/<ENVKEY> (atomic temp+rename, flock). settings.json holds only the env KEY NAMES; the VALUE lives ONLY in the 0600 store. An empty value DELETES (unsets) the reference. SECURITY: the value is NEVER returned — the reply confirms only (agent, key, stored|cleared). 404 unknown_agent for an agent that is not a known BYOA backend (claude).
     */
    setACPSecret(agent: string, key: string, data?: AgentSetACPSecretRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentSetACPSecretResponse>;
    /**
     * Get settings.
     *
     * Returns the effective merged settings (home → project → settings.local.json, top-level merge) plus the home-layer object that settings.patch mutates. Process-wide config (global/no-realm): settings have no realm dimension at any layer, so a per-request realm header returns 400 realm_scope_unsupported.
     */
    getSettings(options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGetSettingsResponse>;
    /**
     * Patch settings.
     *
     * Applies a SHALLOW top-level merge into the home ~/.hoody/settings.json (a nil value deletes a key). Top-level only: sending {"features":{...}} REPLACES the whole features object. Process-wide config (global/no-realm): settings have no realm dimension, so a per-request realm header returns 400 realm_scope_unsupported. SECURITY: security-load-bearing keys (deny_list, deny_urls, features, allowed_directories, hooks, disableAllHooks, mcp_servers, tools) cannot be modified over HTTP and return 403 settings_key_protected — edit settings.json at the host to change a guardrail. A non-null top-level "model" is rejected 400: nothing consumes it (pin a model on the chat agent via PATCH /agents/{name}/model, or switch a live session via PATCH /sessions/{id}/model); {"model":null} still deletes a previously-persisted value. Returns the resulting home object.
     */
    patchSettings(data: AgentPatchSettingsRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentPatchSettingsResponse>;
    /**
     * List fusion composites.
     *
     * Lists the persisted model-fusion composites (fusion.list). Pass ?include_invalid=true to also receive the entries that failed validation as a TOP-LEVEL `invalid` array beside `items` (each with the failure reason and its stable raw-file index; never mixed into or paginated with the selectable items) so a broken composite is editable/deletable. global/no-realm: composites are process-wide config with no realm dimension, so a per-request realm header returns 400 realm_scope_unsupported.
     */
    listFusion(options?: {
        include_invalid?: boolean;
        page?: number;
        limit?: number;
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentListFusionResponse>;
    /**
     * Async iterator helper for paginated operation listFusion.
     */
    listFusionIterator(...args: Parameters<SettingsServiceBase['listFusion']>): AsyncGenerator<unknown, void, unknown>;
    /**
     * Fetch items across pages for operation listFusion.
     *
     * Pass the trailing control object { __allMode: 'iterator' } to receive
     * the async iterator directly instead of an aggregated array.
     */
    listFusionAll(...args: Parameters<SettingsServiceBase['listFusion']>): Promise<unknown[]>;
    listFusionAll(...args: [...Parameters<SettingsServiceBase['listFusion']>, {
        __allMode: 'iterator';
    }]): AsyncGenerator<unknown, void, unknown>;
    /**
     * Create or update a fusion composite.
     *
     * Creates or updates a model-fusion composite keyed by slug (fusion.upsert). The body is the daemon FusionSpec under "spec". The persisted key is derived from spec.name (lowercased, separators collapsed to '-'); the path {slug} must normalize to the same slug or the request is rejected 400 slug_mismatch — the path cannot rename what the body describes. Structural invariants (name/method/members, cycle guard, non-zero context window) are validated server-side.
     */
    upsertFusion(slug: string, data: AgentUpsertFusionRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentUpsertFusionResponse>;
    /**
     * Delete a fusion composite.
     *
     * Removes a model-fusion composite by slug (fusion.delete).
     */
    deleteFusion(slug: string, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentDeleteFusionResponse>;
}
