/**
 * GithubServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in GithubService.
 *
 * Generated at: 2026-07-21T19:46:38.000Z
 */
import { HttpClient } from '../http-client.js';
import type { AgentGithubLoginRequest, AgentGithubLoginResponse, AgentGithubLoginPollRequest, AgentGithubLoginPollResponse, AgentGithubAuthStatusResponse, AgentGithubBranchesResponse, AgentGithubCloneRequest, AgentGithubCloneResponse, AgentGithubCommitRequest, AgentGithubCommitResponse, AgentGithubPullRequestRequest, AgentGithubPullRequestResponse, AgentGithubReposResponse, AgentGithubStatusResponse, AgentGithubSyncRequest, AgentGithubSyncResponse } from '../types.js';
export declare abstract class GithubServiceBase {
    private defaultUrlTemplateVariables?;
    protected readonly http: HttpClient;
    protected readonly _kitNamespace: string | undefined;
    private readonly urlTemplatePattern;
    constructor(http: HttpClient, namespace?: string, defaultUrlTemplateVariables?: Record<string, string | number> | undefined, urlTemplatePattern?: string);
    /**
     * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
     */
    private _wrapHttpClient;
    /**
     * Build URL with template variables or fallback to baseURL
     *
     * @param path - API endpoint path
     * @param variables - Method-level template variables to override defaults
     * @returns Full URL (template mode) or path only (baseURL mode)
     */
    protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string;
    /**
     * Read nested values from objects using dotted paths.
     * Example: "data.items" or "data.pagination.total"
     */
    protected getPathValue(input: unknown, path: string): unknown;
    /**
     * Start a GitHub device-flow login (or add a PAT).
     *
     * Begins a GitHub login. With NO body token this starts a DEVICE flow and returns the non-secret {device_code, user_code, verification_uri, interval, expires_in}; the user authorizes that URL out of band, then POST /github/auth/login/poll completes the login. With a body `token` (a PAT) this instead validates + persists the token directly (no device flow), returning the linked {login, host}. For GitHub Enterprise (GHES) set body `host` (default github.com) on BOTH the device-flow start and the PAT-add. SECURITY: the token lives only in env / the validate call and is never returned.
     */
    githubLogin(data?: AgentGithubLoginRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubLoginResponse>;
    /**
     * Poll a GitHub device-flow login to completion.
     *
     * Polls an in-flight GitHub device-flow login. Forward the {device_code, interval, expires_in} the start reply returned; the call blocks until the user authorizes (or the flow expires/cancels), then validates + persists the resulting token. Required to COMPLETE a device-flow login over HTTP (the start call only returns the verification URL / device code) — BLOCK. SECURITY: the token lives only in env and is never returned; only the linked {login, host} is echoed.
     */
    githubLoginPoll(data: AgentGithubLoginPollRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubLoginPollResponse>;
    /**
     * GitHub auth status.
     *
     * Reports the GitHub authentication state (github.auth.status) from the account store. Token only in env; never returned. It has NO realm/container/cwd/config_dir dimension (unlike the cwd-scoped github status/repos/branches reads), so EVERY per-request scope header returns 400 realm_scope_unsupported. The device-flow login (githubLogin) and the clone/commit/sync/pr mutators (githubClone/githubCommit/githubSync/githubPullRequest) are write routes.
     */
    githubAuthStatus(options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubAuthStatusResponse>;
    /**
     * List GitHub branches.
     *
     * Lists branches for the requesting cwd's repo (github.branch.list). Returns {status, branches} verbatim; not paginated. cwd-scoped, not realm-scoped — a per-request realm header returns 400 realm_scope_unsupported.
     */
    githubBranches(options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubBranchesResponse>;
    /**
     * Clone a GitHub repository.
     *
     * Clones a GitHub repository. The service translates `repo` (owner/name OR an https github URL) into the server-authoritative {full_name, clone_url}: the clone runs against the ACTIVE account host (a URL whose host does not match is rejected), and the destination is derived traversal-safe under the managed clone root (`dir` overrides it). Destructive (x-cli-mode interactive).
     */
    githubClone(data?: AgentGithubCloneRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubCloneResponse>;
    /**
     * Stage all and commit.
     *
     * Stages all changes and commits in the requesting cwd's repo. Destructive.
     */
    githubCommit(data: AgentGithubCommitRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubCommitResponse>;
    /**
     * Open a pull request.
     *
     * Opens a pull request for the requesting cwd's repo. Destructive.
     */
    githubPullRequest(data: AgentGithubPullRequestRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubPullRequestResponse>;
    /**
     * List GitHub repos.
     *
     * Lists known/configured repos (github.repo.list). Returns the daemon reply verbatim ({status, repos, account}); not paginated.
     */
    githubRepos(options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubReposResponse>;
    /**
     * GitHub working-tree status.
     *
     * Returns the git working-tree status (github.status) for the requesting cwd. cwd-scoped (git is repo/cwd-relative), not realm-scoped — a per-request realm header returns 400 realm_scope_unsupported.
     */
    githubStatus(options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubStatusResponse>;
    /**
     * Sync (fetch → pull → push).
     *
     * Runs fetch → pull → push as one logical sync, stopping at the FIRST non-ok step so a later push never runs against a half-synced tree. The body `direction` lets a caller pull-only (fetch+pull) or push-only; default is the full sync (this is BODY-only — there is no ?direction alias). Destructive. 502 sync_step_failed carries the steps that ran plus the failing one under details.
     */
    githubSync(data?: AgentGithubSyncRequest, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGithubSyncResponse>;
}
