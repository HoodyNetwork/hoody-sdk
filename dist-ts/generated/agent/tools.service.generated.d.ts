/**
 * ToolsServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in ToolsService.
 *
 * Generated at: 2026-08-06T22:43:11.000Z
 */
import { HttpClient } from '../http-client.js';
import type { AgentListSessionToolsResponse, AgentListSessionMCPToolsResponse, AgentRunSessionToolRequest, AgentRunSessionToolResponse, AgentListToolsResponse, AgentListReadOnlyToolsResponse, AgentGetToolResponse, AgentRunToolRequest, AgentRunToolResponse, AgentRunToolAsyncRequest, AgentRunToolAsyncResponse, AgentStreamToolRequest } from '../types.js';
import { AgentStreamToolWebSocket } from './agent_stream-tool.websocket.js';
export declare abstract class ToolsServiceBase {
    private defaultUrlTemplateVariables?;
    protected readonly http: HttpClient;
    protected readonly _kitNamespace: string | undefined;
    private readonly urlTemplatePattern;
    constructor(http: HttpClient, namespace?: string, defaultUrlTemplateVariables?: Record<string, string | number> | undefined, urlTemplatePattern?: string);
    /**
     * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
     */
    private _wrapHttpClient;
    /**
     * Build URL with template variables or fallback to baseURL
     *
     * @param path - API endpoint path
     * @param variables - Method-level template variables to override defaults
     * @returns Full URL (template mode) or path only (baseURL mode)
     */
    protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string;
    /**
     * Read nested values from objects using dotted paths.
     * Example: "data.items" or "data.pagination.total"
     */
    protected getPathValue(input: unknown, path: string): unknown;
    /**
     * List a session&#x27;s effective tool set.
     *
     * Returns the complete effective tool set for a LIVE session (computeTools = config-enabled AND on PATH, gated by the session's tool mode / agent allowlist / headless exclusions). 404 when the session is not live.
     */
    listSessionTools(id: string, options?: {
        page?: number;
        limit?: number;
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentListSessionToolsResponse>;
    /**
     * Async iterator helper for paginated operation listSessionTools.
     */
    listSessionToolsIterator(...args: Parameters<ToolsServiceBase['listSessionTools']>): AsyncGenerator<unknown, void, unknown>;
    /**
     * Fetch items across pages for operation listSessionTools.
     *
     * Pass the trailing control object { __allMode: 'iterator' } to receive
     * the async iterator directly instead of an aggregated array.
     */
    listSessionToolsAll(...args: Parameters<ToolsServiceBase['listSessionTools']>): Promise<unknown[]>;
    listSessionToolsAll(...args: [...Parameters<ToolsServiceBase['listSessionTools']>, {
        __allMode: 'iterator';
    }]): AsyncGenerator<unknown, void, unknown>;
    /**
     * List a session&#x27;s MCP tools.
     *
     * Returns the MCP (mcp__*) tools available in a LIVE session (drawn from the same computeTools snapshot). 404 when the session is not live.
     */
    listSessionMCPTools(id: string, options?: {
        page?: number;
        limit?: number;
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentListSessionMCPToolsResponse>;
    /**
     * Async iterator helper for paginated operation listSessionMCPTools.
     */
    listSessionMCPToolsIterator(...args: Parameters<ToolsServiceBase['listSessionMCPTools']>): AsyncGenerator<unknown, void, unknown>;
    /**
     * Fetch items across pages for operation listSessionMCPTools.
     *
     * Pass the trailing control object { __allMode: 'iterator' } to receive
     * the async iterator directly instead of an aggregated array.
     */
    listSessionMCPToolsAll(...args: Parameters<ToolsServiceBase['listSessionMCPTools']>): Promise<unknown[]>;
    listSessionMCPToolsAll(...args: [...Parameters<ToolsServiceBase['listSessionMCPTools']>, {
        __allMode: 'iterator';
    }]): AsyncGenerator<unknown, void, unknown>;
    /**
     * Run a tool inside a live session (gated).
     *
     * Runs a tool through the gated policy gate on a LIVE session, against the session's frozen realm/container/cwd/tool-mode/dir-scope, with the full permission checks. It claims the session's single serial turn slot so a direct tool run never races the turn loop (409 turn_in_flight if a turn is running; 409 gate_parked if a gate is parked). Because it occupies the turn slot it is also CANCELLABLE the same way a turn is: a concurrent POST /sessions/{id}/cancel (or a WS cancel frame, or session close) aborts the in-flight run. A parked confirmation returns 409 tool_needs_confirmation with the echoed params; re-issue with confirm:true. 404 tool_not_found when the tool is not in the session's effective tool list. NOTE (headless-live exception): a session CREATED with headless:true HARD-DENIES the dangerous bash shapes (rm -rf /, curl|sh, git push --force) and outside-cwd file writes (200 is_error, no 409 confirm round) — those confirm-gated shapes are parkable only on a non-headless session or the sessionless ephemeral path; a headless live session has no interactive confirmer for them.
     */
    runSessionTool(id: string, name: string, data?: AgentRunSessionToolRequest, options?: {
        confirm?: boolean;
        confirm_token?: string;
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentRunSessionToolResponse>;
    /**
     * List the tool catalogue.
     *
     * Lists the agent's built-in tool schemas (name, description, JSON-Schema input, read_only flag). Catalogue-wide and realm-independent: a per-request realm header is rejected. Pagination: pass ?page=N&limit=M for a window; omit limit for the full set.
     */
    listTools(options?: {
        page?: number;
        limit?: number;
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentListToolsResponse>;
    /**
     * Async iterator helper for paginated operation listTools.
     */
    listToolsIterator(...args: Parameters<ToolsServiceBase['listTools']>): AsyncGenerator<unknown, void, unknown>;
    /**
     * Fetch items across pages for operation listTools.
     *
     * Pass the trailing control object { __allMode: 'iterator' } to receive
     * the async iterator directly instead of an aggregated array.
     */
    listToolsAll(...args: Parameters<ToolsServiceBase['listTools']>): Promise<unknown[]>;
    listToolsAll(...args: [...Parameters<ToolsServiceBase['listTools']>, {
        __allMode: 'iterator';
    }]): AsyncGenerator<unknown, void, unknown>;
    /**
     * List the read-only tool subset.
     *
     * Lists the read-only tools (safe for planning / sessionless runs). Catalogue-wide and realm-independent: a per-request realm header is rejected. Pagination honored.
     */
    listReadOnlyTools(options?: {
        page?: number;
        limit?: number;
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentListReadOnlyToolsResponse>;
    /**
     * Async iterator helper for paginated operation listReadOnlyTools.
     */
    listReadOnlyToolsIterator(...args: Parameters<ToolsServiceBase['listReadOnlyTools']>): AsyncGenerator<unknown, void, unknown>;
    /**
     * Fetch items across pages for operation listReadOnlyTools.
     *
     * Pass the trailing control object { __allMode: 'iterator' } to receive
     * the async iterator directly instead of an aggregated array.
     */
    listReadOnlyToolsAll(...args: Parameters<ToolsServiceBase['listReadOnlyTools']>): Promise<unknown[]>;
    listReadOnlyToolsAll(...args: [...Parameters<ToolsServiceBase['listReadOnlyTools']>, {
        __allMode: 'iterator';
    }]): AsyncGenerator<unknown, void, unknown>;
    /**
     * Get one tool schema.
     *
     * Returns the schema for a single built-in tool by name. Catalogue-wide and realm-independent: a per-request realm header is rejected. 404 tool_not_found for an unknown name.
     */
    getTool(name: string, options?: {
        realm?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentGetToolResponse>;
    /**
     * Run a tool (sessionless, gated).
     *
     * Runs a tool through the single gated policy gate with NO open session: creates an ephemeral local session from the scope headers (X-Hoody-Cwd / X-Hoody-Config-Dir), and runs the full permission checks. Sessionless runs are read-only by default — a non-read-only tool requires allow_mutations:true or confirm:true (else 400 tool_mutation_refused). A parked confirmation returns 409 tool_needs_confirmation with the echoed tool+params; re-issue with confirm:true. FILESYSTEM REACH: a sessionless run resolves paths under the caller-supplied X-Hoody-Cwd subtree (dir-scope defaults to home/restricted-to-cwd).
     */
    runTool(name: string, data?: AgentRunToolRequest, options?: {
        confirm?: boolean;
        confirm_token?: string;
        realm?: string;
        XHoodyToolMode?: string;
        XHoodyDirScope?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentRunToolResponse>;
    /**
     * Run a tool asynchronously (sessionless, gated).
     *
     * Async form of runTool: returns {job_id} immediately and runs the gated tool in the background. Poll GET /jobs/{id} / GET /jobs/{id}/result for the result (or the parked confirmation). A non-read-only sessionless run still requires an explicit posture.
     */
    runToolAsync(name: string, data?: AgentRunToolAsyncRequest, options?: {
        confirm?: boolean;
        confirm_token?: string;
        realm?: string;
        XHoodyToolMode?: string;
        XHoodyDirScope?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentRunToolAsyncResponse>;
    /**
     * Run a tool with a streamed result (sessionless, gated).
     *
     * Streaming form of runTool over Server-Sent Events: a `start` frame, then a `result` / `needs_confirmation` / `error` frame, then `end`. The work is a one-shot gated RPC, so there is no per-token stream — the SSE shape mirrors the kit streaming convention for symmetry with the session stream. A non-read-only sessionless run still requires an explicit posture.
     */
    streamTool(name: string, data?: AgentStreamToolRequest, options?: {
        confirm?: boolean;
        confirm_token?: string;
        realm?: string;
        XHoodyToolMode?: string;
        XHoodyDirScope?: string;
        XHoodyCwd?: string;
        XHoodyConfigDir?: string;
        XHoodyContainer?: string;
        XHoodyRealm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }, _templateVars?: {
        projectId?: string;
        containerId?: string;
        serviceIndex?: string | number;
        server?: string;
    }): Promise<AgentStreamToolWebSocket>;
}
