/**
 * KvStoreServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in KvStoreService.
 *
 * Generated at: 2026-07-23T00:21:26.000Z
 */
import { ValidationError } from '../errors.js';
export class KvStoreServiceBase {
    defaultUrlTemplateVariables;
    http;
    _kitNamespace;
    urlTemplatePattern;
    constructor(http, namespace, defaultUrlTemplateVariables, urlTemplatePattern) {
        this.defaultUrlTemplateVariables = defaultUrlTemplateVariables;
        this._kitNamespace = namespace;
        this.http = namespace ? this._wrapHttpClient(http, namespace) : http;
        this.urlTemplatePattern = urlTemplatePattern;
    }
    /**
     * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
     */
    _wrapHttpClient(http, ns) {
        return new Proxy(http, {
            get: (target, prop) => {
                const val = target[prop];
                if (typeof val === 'function' && ['get', 'post', 'put', 'patch', 'delete', 'head', 'options', 'request'].includes(prop)) {
                    return (...args) => {
                        const dataIndex = prop === 'request' ? 2 : 1;
                        const data = args[dataIndex] ? { ...args[dataIndex] } : {};
                        data.middlewareContext = { ...data.middlewareContext, _kitNamespace: ns };
                        args[dataIndex] = data;
                        return val.apply(target, args);
                    };
                }
                return typeof val === 'function' ? val.bind(target) : val;
            }
        });
    }
    /**
     * Build URL with template variables or fallback to baseURL
     *
     * @param path - API endpoint path
     * @param variables - Method-level template variables to override defaults
     * @returns Full URL (template mode) or path only (baseURL mode)
     */
    buildTemplateUrl(path, variables) {
        // Get URL pattern: client override > default template pattern
        const urlPattern = this.urlTemplatePattern || 'https://{projectId}-{containerId}-sqlite-{serviceIndex}.{server}.containers.hoody.com';
        // Merge variables: method level > client level
        const allVariables = { ...this.defaultUrlTemplateVariables, ...variables };
        const hasVariables = Object.keys(allVariables).length > 0;
        if (!hasVariables) {
            // BaseURL mode: No template variables provided
            // Return path only - HttpClient will prepend baseURL automatically
            return path;
        }
        // Template mode: Build full URL with variable substitution
        let url = urlPattern;
        for (const [key, value] of Object.entries(allVariables)) {
            if (value !== undefined) {
                url = url.replace(`{${key}}`, () => String(value));
            }
        }
        // Validate all variables were replaced (safety check)
        if (url.includes('{') && url.includes('}')) {
            const processRef = globalThis.process;
            if (processRef?.env?.SDK_DEBUG) {
                console.warn(`[Hoody SDK] URL template has unreplaced variables: ${url}`);
                console.warn(`[Hoody SDK] Falling back to baseURL mode.`);
            }
            // Fallback to baseURL mode if template incomplete
            return path;
        }
        return `${url}${path}`;
    }
    /**
     * Read nested values from objects using dotted paths.
     * Example: "data.items" or "data.pagination.total"
     */
    getPathValue(input, path) {
        if (!path)
            return undefined;
        const segments = path.split('.').filter(Boolean);
        let current = input;
        for (const segment of segments) {
            if (current === undefined || current === null) {
                return undefined;
            }
            if (Array.isArray(current) && segment === 'length') {
                current = current.length;
                continue;
            }
            if (typeof current !== 'object') {
                return undefined;
            }
            current = current[segment];
        }
        return current;
    }
    /**
     * List keys
     *
     * List all keys in the KV store with optional filtering and pagination
     */
    async list(options, _templateVars) {
        const { db, table, prefix, limit, offset, at_timestamp, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for prefix
        if (prefix !== undefined && prefix !== null) {
        }
        // Runtime constraint validation for limit
        if (limit !== undefined && limit !== null) {
            if (!Number.isFinite(limit) || !Number.isInteger(limit)) {
                throw new ValidationError('limit must be an integer', 'limit');
            }
        }
        // Runtime constraint validation for offset
        if (offset !== undefined && offset !== null) {
            if (!Number.isFinite(offset) || !Number.isInteger(offset)) {
                throw new ValidationError('offset must be an integer', 'offset');
            }
        }
        // Runtime constraint validation for at_timestamp
        if (at_timestamp !== undefined && at_timestamp !== null) {
            if (!Number.isFinite(at_timestamp) || !Number.isInteger(at_timestamp)) {
                throw new ValidationError('at_timestamp must be an integer', 'at_timestamp');
            }
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv`, _templateVars || {});
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (prefix !== undefined) {
            requestData.query['prefix'] = prefix;
        }
        if (limit !== undefined) {
            requestData.query['limit'] = limit;
        }
        if (offset !== undefined) {
            requestData.query['offset'] = offset;
        }
        if (at_timestamp !== undefined) {
            requestData.query['at_timestamp'] = at_timestamp;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
    /**
     * Async iterator helper for paginated operation list.
     */
    async *listIterator(...args) {
        const paginationType = 'page-number';
        const itemsPath = 'data.items';
        const totalPath = 'data.count';
        const nextCursorPath = 'data.next_cursor';
        const hasMorePath = 'data.has_more';
        const limitParam = 'limit';
        const offsetParam = 'offset';
        const requestArgs = [...args];
        const optionsIndex = 0;
        const initialOptions = (requestArgs[optionsIndex] || {});
        const rawInitialOffset = initialOptions[offsetParam];
        const parsedInitialOffset = typeof rawInitialOffset === 'number'
            ? rawInitialOffset
            : (typeof rawInitialOffset === 'string' && /^\d+$/.test(rawInitialOffset.trim())
                ? Number(rawInitialOffset.trim())
                : Number.NaN);
        let hasNext = true;
        let currentOffset = Number.isFinite(parsedInitialOffset)
            ? Number(parsedInitialOffset)
            : (paginationType.includes('page') ? 1 : 0);
        let cursor;
        let pageCount = 0;
        let fetchedCount = 0;
        const maxPages = 1000;
        while (hasNext && pageCount < maxPages) {
            pageCount += 1;
            // Honour caller AbortSignal between pages. Stops mid-way through
            // long paginations (up to maxPages = 1000) instead of blocking.
            // requestOptions can land at any trailing position depending on the
            // operation shape (data / templateVars / opts). Scan for any arg that
            // carries a signal that has fired.
            for (const maybeOpts of requestArgs) {
                const sig = maybeOpts?.signal;
                if (sig && sig.aborted) {
                    const reason = sig.reason;
                    throw reason instanceof Error
                        ? reason
                        : new Error('Paginator aborted before fetching next page.');
                }
            }
            const options = {
                ...(requestArgs[optionsIndex] || {}),
            };
            if (cursor !== undefined) {
                options[offsetParam] = cursor;
            }
            else {
                options[offsetParam] = currentOffset;
            }
            if (!Object.prototype.hasOwnProperty.call(options, limitParam) && '100') {
                options[limitParam] = Number('100');
            }
            requestArgs[optionsIndex] = options;
            const response = await this.list(...requestArgs);
            const items = this.getPathValue(response, itemsPath);
            const normalizedItems = Array.isArray(items)
                ? items
                : (items === undefined || items === null ? [] : [items]);
            fetchedCount += normalizedItems.length;
            for (const item of normalizedItems) {
                yield item;
            }
            if (normalizedItems.length === 0) {
                break;
            }
            const hasMore = this.getPathValue(response, hasMorePath);
            const nextCursor = this.getPathValue(response, nextCursorPath);
            const total = this.getPathValue(response, totalPath);
            if (typeof hasMore === 'boolean') {
                hasNext = hasMore;
            }
            else if (typeof nextCursor === 'string' && nextCursor.length > 0) {
                hasNext = true;
            }
            else if (typeof total === 'number' && Number.isFinite(total)) {
                if (paginationType.includes('page')) {
                    hasNext = fetchedCount < total;
                }
                else {
                    const nextOffset = currentOffset + normalizedItems.length;
                    hasNext = nextOffset < total;
                }
            }
            else {
                hasNext = normalizedItems.length > 0;
            }
            if (typeof nextCursor === 'string' && nextCursor.length > 0) {
                cursor = nextCursor;
            }
            else {
                cursor = undefined;
            }
            if (cursor === undefined && normalizedItems.length > 0) {
                currentOffset += paginationType.includes('page') ? 1 : normalizedItems.length;
            }
        }
    }
    listAll(...args) {
        const expectedArgCount = 0 + 1;
        const maybeControl = args.length > 0
            ? args[args.length - 1]
            : undefined;
        const controlMode = (maybeControl
            && typeof maybeControl === 'object'
            && !Array.isArray(maybeControl)
            && maybeControl.__allMode);
        const isOnlyControlObject = Boolean(controlMode !== undefined
            && maybeControl
            && Object.keys(maybeControl).every((key) => key === '__allMode'));
        const hasControl = Boolean((args.length > expectedArgCount && controlMode !== undefined)
            || (args.length === expectedArgCount && isOnlyControlObject));
        const requestArgs = (hasControl
            ? args.slice(0, -1)
            : args);
        if (!hasControl && requestArgs.length > 0) {
            const optionsArgIndex = 0;
            const optionsArg = requestArgs[optionsArgIndex];
            if (optionsArg
                && typeof optionsArg === 'object'
                && !Array.isArray(optionsArg)
                && Object.prototype.hasOwnProperty.call(optionsArg, '__allMode')) {
                const sanitizedOptions = { ...optionsArg };
                delete sanitizedOptions.__allMode;
                requestArgs[optionsArgIndex] = sanitizedOptions;
            }
        }
        const iterator = this.listIterator(...requestArgs);
        if (hasControl && controlMode === 'iterator') {
            return iterator;
        }
        return (async () => {
            const items = [];
            for await (const item of iterator) {
                items.push(item);
            }
            return items;
        })();
    }
    /**
     * Get value by key
     *
     * Retrieve a value from the KV store with optional JSON path extraction and time-travel support
     */
    async get(key, options, _templateVars) {
        const { db, table, path, at_timestamp, rebuild, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for path
        if (path !== undefined && path !== null) {
        }
        // Runtime constraint validation for at_timestamp
        if (at_timestamp !== undefined && at_timestamp !== null) {
            if (!Number.isFinite(at_timestamp) || !Number.isInteger(at_timestamp)) {
                throw new ValidationError('at_timestamp must be an integer', 'at_timestamp');
            }
        }
        // Runtime constraint validation for rebuild
        if (rebuild !== undefined && rebuild !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (path !== undefined) {
            requestData.query['path'] = path;
        }
        if (at_timestamp !== undefined) {
            requestData.query['at_timestamp'] = at_timestamp;
        }
        if (rebuild !== undefined) {
            requestData.query['rebuild'] = rebuild;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
    /**
     * Set value for key
     *
     * Store or update a value in the KV store with optional TTL, JSON path, and compare-and-swap
     */
    async set(key, data, options, _templateVars) {
        const { db, table, path, ttl, if_match, history, create_db_if_missing, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: data
        if (data === undefined || data === null) {
            throw new ValidationError('data is required', 'data');
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for path
        if (path !== undefined && path !== null) {
        }
        // Runtime constraint validation for ttl
        if (ttl !== undefined && ttl !== null) {
            if (!Number.isFinite(ttl) || !Number.isInteger(ttl)) {
                throw new ValidationError('ttl must be an integer', 'ttl');
            }
        }
        // Runtime constraint validation for if_match
        if (if_match !== undefined && if_match !== null) {
        }
        // Runtime constraint validation for history
        if (history !== undefined && history !== null) {
        }
        // Runtime constraint validation for create_db_if_missing
        if (create_db_if_missing !== undefined && create_db_if_missing !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        requestData.body = data;
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (path !== undefined) {
            requestData.query['path'] = path;
        }
        if (ttl !== undefined) {
            requestData.query['ttl'] = ttl;
        }
        if (if_match !== undefined) {
            requestData.query['if_match'] = if_match;
        }
        if (history !== undefined) {
            requestData.query['history'] = history;
        }
        if (create_db_if_missing !== undefined) {
            requestData.query['create_db_if_missing'] = create_db_if_missing;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.put(requestUrl, requestData);
    }
    /**
     * Delete key
     *
     * Remove a key-value pair from the store
     */
    async delete(key, options, _templateVars) {
        const { db, table, history, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for history
        if (history !== undefined && history !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (history !== undefined) {
            requestData.query['history'] = history;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.delete(requestUrl, requestData);
    }
    /**
     * Check if key exists
     *
     * Check if a key exists in the KV store without retrieving its value
     */
    async exists(key, options, _templateVars) {
        const { db, table, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.head(requestUrl, requestData);
    }
    /**
     * Atomic decrement
     *
     * Atomically decrement a numeric value (supports JSON paths for nested values)
     */
    async decr(key, options, _templateVars) {
        const { db, table, delta, path, history, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for delta
        if (delta !== undefined && delta !== null) {
            if (!Number.isFinite(delta) || !Number.isInteger(delta)) {
                throw new ValidationError('delta must be an integer', 'delta');
            }
        }
        // Runtime constraint validation for path
        if (path !== undefined && path !== null) {
        }
        // Runtime constraint validation for history
        if (history !== undefined && history !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/decr`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (delta !== undefined) {
            requestData.query['delta'] = delta;
        }
        if (path !== undefined) {
            requestData.query['path'] = path;
        }
        if (history !== undefined) {
            requestData.query['history'] = history;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Get key operation history
     *
     * Retrieve the operation history for a specific key showing all changes over time
     */
    async getHistory(key, options, _templateVars) {
        const { db, table, limit, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for limit
        if (limit !== undefined && limit !== null) {
            if (!Number.isFinite(limit) || !Number.isInteger(limit)) {
                throw new ValidationError('limit must be an integer', 'limit');
            }
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/history`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (limit !== undefined) {
            requestData.query['limit'] = limit;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
    /**
     * Atomic increment
     *
     * Atomically increment a numeric value (supports JSON paths for nested values)
     */
    async incr(key, options, _templateVars) {
        const { db, table, delta, path, history, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for delta
        if (delta !== undefined && delta !== null) {
            if (!Number.isFinite(delta) || !Number.isInteger(delta)) {
                throw new ValidationError('delta must be an integer', 'delta');
            }
        }
        // Runtime constraint validation for path
        if (path !== undefined && path !== null) {
        }
        // Runtime constraint validation for history
        if (history !== undefined && history !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/incr`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (delta !== undefined) {
            requestData.query['delta'] = delta;
        }
        if (path !== undefined) {
            requestData.query['path'] = path;
        }
        if (history !== undefined) {
            requestData.query['history'] = history;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Remove from array end
     *
     * Pop the last element from an array (supports JSON paths for nested arrays)
     */
    async pop(key, options, _templateVars) {
        const { db, table, path, history, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for path
        if (path !== undefined && path !== null) {
        }
        // Runtime constraint validation for history
        if (history !== undefined && history !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/pop`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (path !== undefined) {
            requestData.query['path'] = path;
        }
        if (history !== undefined) {
            requestData.query['history'] = history;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Append to array
     *
     * Append a value to an array (supports JSON paths for nested arrays)
     */
    async push(key, data, options, _templateVars) {
        const { db, table, path, history, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: data
        if (data === undefined || data === null) {
            throw new ValidationError('data is required', 'data');
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for path
        if (path !== undefined && path !== null) {
        }
        // Runtime constraint validation for history
        if (history !== undefined && history !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/push`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        requestData.body = data;
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (path !== undefined) {
            requestData.query['path'] = path;
        }
        if (history !== undefined) {
            requestData.query['history'] = history;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Remove array element
     *
     * Remove an element from an array by index or value (supports JSON paths)
     */
    async removeElement(key, data, options, _templateVars) {
        const { db, table, path, index, history, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: data
        if (data === undefined || data === null) {
            throw new ValidationError('data is required', 'data');
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for path
        if (path !== undefined && path !== null) {
        }
        // Runtime constraint validation for index
        if (index !== undefined && index !== null) {
            if (!Number.isFinite(index) || !Number.isInteger(index)) {
                throw new ValidationError('index must be an integer', 'index');
            }
        }
        // Runtime constraint validation for history
        if (history !== undefined && history !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/remove`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        requestData.body = data;
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (path !== undefined) {
            requestData.query['path'] = path;
        }
        if (index !== undefined) {
            requestData.query['index'] = index;
        }
        if (history !== undefined) {
            requestData.query['history'] = history;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Rollback key operations
     *
     * Rollback a key to its previous state by undoing the last N operations
     */
    async rollback(key, options, _templateVars) {
        const { db, table, steps, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for steps
        if (steps !== undefined && steps !== null) {
            if (!Number.isFinite(steps) || !Number.isInteger(steps)) {
                throw new ValidationError('steps must be an integer', 'steps');
            }
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/rollback`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (steps !== undefined) {
            requestData.query['steps'] = steps;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Get key snapshot at operation
     *
     * Reconstruct the value of a key as it was at a specific operation number
     */
    async getSnapshot(key, options, _templateVars) {
        const { db, op_number, table, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: key
        if (key === undefined || key === null) {
            throw new ValidationError('key is required', 'key');
        }
        // Runtime constraint validation for key
        if (key !== undefined && key !== null) {
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Validate required parameter: op_number
        if (op_number === undefined || op_number === null) {
            throw new ValidationError('op_number is required', 'op_number');
        }
        // Runtime constraint validation for op_number
        if (op_number !== undefined && op_number !== null) {
            if (!Number.isFinite(op_number) || !Number.isInteger(op_number)) {
                throw new ValidationError('op_number must be an integer', 'op_number');
            }
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/{key}/snapshot`, _templateVars || {});
        // Replace path parameters
        requestUrl = requestUrl.replace('{' + 'key' + '}', () => encodeURIComponent(String(key)));
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (op_number !== undefined) {
            requestData.query['op_number'] = op_number;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
    /**
     * Batch delete multiple keys
     *
     * Delete multiple keys in a single transaction (max 100 keys)
     */
    async batchDelete(data, options, _templateVars) {
        const { db, table, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: data
        if (data === undefined || data === null) {
            throw new ValidationError('data is required', 'data');
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/batch/delete`, _templateVars || {});
        const requestData = {};
        requestData.body = data;
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Batch get multiple keys
     *
     * Retrieve values for multiple keys in a single request (max 100 keys)
     */
    async batchGet(data, options, _templateVars) {
        const { db, table, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: data
        if (data === undefined || data === null) {
            throw new ValidationError('data is required', 'data');
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/batch/get`, _templateVars || {});
        const requestData = {};
        requestData.body = data;
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Batch set multiple keys
     *
     * Store values for multiple keys in a single transaction (max 100 items)
     */
    async batchSet(data, options, _templateVars) {
        const { db, table, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: data
        if (data === undefined || data === null) {
            throw new ValidationError('data is required', 'data');
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/batch/set`, _templateVars || {});
        const requestData = {};
        requestData.body = data;
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Compare table snapshots
     *
     * Compare the KV table state between two timestamps showing created, modified, and deleted keys
     */
    async compareSnapshots(options, _templateVars) {
        const { db, from, to, table, keys, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Validate required parameter: from
        if (from === undefined || from === null) {
            throw new ValidationError('from is required', 'from');
        }
        // Runtime constraint validation for from
        if (from !== undefined && from !== null) {
            if (!Number.isFinite(from) || !Number.isInteger(from)) {
                throw new ValidationError('from must be an integer', 'from');
            }
        }
        // Validate required parameter: to
        if (to === undefined || to === null) {
            throw new ValidationError('to is required', 'to');
        }
        // Runtime constraint validation for to
        if (to !== undefined && to !== null) {
            if (!Number.isFinite(to) || !Number.isInteger(to)) {
                throw new ValidationError('to must be an integer', 'to');
            }
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for keys
        if (keys !== undefined && keys !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/diff`, _templateVars || {});
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (from !== undefined) {
            requestData.query['from'] = from;
        }
        if (to !== undefined) {
            requestData.query['to'] = to;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (keys !== undefined) {
            requestData.query['keys'] = keys;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
    /**
     * Rollback entire table
     *
     * Rollback the entire KV table to a specific timestamp (requires confirmation)
     */
    async rollbackTable(data, options, _templateVars) {
        const { db, to_timestamp, table, dry_run, confirm, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: data
        if (data === undefined || data === null) {
            throw new ValidationError('data is required', 'data');
        }
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Validate required parameter: to_timestamp
        if (to_timestamp === undefined || to_timestamp === null) {
            throw new ValidationError('to_timestamp is required', 'to_timestamp');
        }
        // Runtime constraint validation for to_timestamp
        if (to_timestamp !== undefined && to_timestamp !== null) {
            if (!Number.isFinite(to_timestamp) || !Number.isInteger(to_timestamp)) {
                throw new ValidationError('to_timestamp must be an integer', 'to_timestamp');
            }
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for dry_run
        if (dry_run !== undefined && dry_run !== null) {
        }
        // Runtime constraint validation for confirm
        if (confirm !== undefined && confirm !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/rollback`, _templateVars || {});
        const requestData = {};
        requestData.body = data;
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (to_timestamp !== undefined) {
            requestData.query['to_timestamp'] = to_timestamp;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (dry_run !== undefined) {
            requestData.query['dry_run'] = dry_run;
        }
        if (confirm !== undefined) {
            requestData.query['confirm'] = confirm;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
    /**
     * Get table snapshot at timestamp
     *
     * Reconstruct the entire KV table state as it was at a specific timestamp
     */
    async getTableSnapshot(options, _templateVars) {
        const { db, timestamp, table, limit, prefix, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Validate required parameter: db
        if (db === undefined || db === null) {
            throw new ValidationError('db is required', 'db');
        }
        // Runtime constraint validation for db
        if (db !== undefined && db !== null) {
        }
        // Validate required parameter: timestamp
        if (timestamp === undefined || timestamp === null) {
            throw new ValidationError('timestamp is required', 'timestamp');
        }
        // Runtime constraint validation for timestamp
        if (timestamp !== undefined && timestamp !== null) {
            if (!Number.isFinite(timestamp) || !Number.isInteger(timestamp)) {
                throw new ValidationError('timestamp must be an integer', 'timestamp');
            }
        }
        // Runtime constraint validation for table
        if (table !== undefined && table !== null) {
        }
        // Runtime constraint validation for limit
        if (limit !== undefined && limit !== null) {
            if (!Number.isFinite(limit) || !Number.isInteger(limit)) {
                throw new ValidationError('limit must be an integer', 'limit');
            }
        }
        // Runtime constraint validation for prefix
        if (prefix !== undefined && prefix !== null) {
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/sqlite/kv/snapshot`, _templateVars || {});
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (db !== undefined) {
            requestData.query['db'] = db;
        }
        if (timestamp !== undefined) {
            requestData.query['timestamp'] = timestamp;
        }
        if (table !== undefined) {
            requestData.query['table'] = table;
        }
        if (limit !== undefined) {
            requestData.query['limit'] = limit;
        }
        if (prefix !== undefined) {
            requestData.query['prefix'] = prefix;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
}
