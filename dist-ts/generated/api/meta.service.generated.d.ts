/**
 * MetaServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in MetaService.
 *
 * Generated at: 2026-08-05T22:37:04.000Z
 */
import { HttpClient } from '../http-client.js';
import type { ApiMetaGetPublicKeyResponse, GetSocialStatsResponse } from '../types.js';
export declare abstract class MetaServiceBase {
    protected readonly http: HttpClient;
    protected readonly _kitNamespace: string | undefined;
    protected readonly defaultRealmId: string | undefined;
    constructor(http: HttpClient, namespace?: string, defaultRealmId?: string);
    /**
     * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
     */
    private _wrapHttpClient;
    /**
     * Build URL with realm support
     * @param path - Base path
     * @param realmId - Realm ID (per-operation override or SDK default)
     * @param realmConfig - Effective realm routing configuration for this call
     * @returns Full URL with realm subdomain if applicable
     */
    protected buildRealmUrl(path: string, realmId?: string, realmConfig?: {
        optional: boolean;
        baseDomain: string;
        subdomainPattern: string;
        parameterName: string;
    }): string;
    /**
     * Read nested values from objects using dotted paths.
     * Example: "data.items" or "data.pagination.total"
     */
    protected getPathValue(input: unknown, path: string): unknown;
    /**
     * Get Hoody API Signing Public Key
     *
     * Returns the ED25519 public key(s) used by Hoody to sign all API responses
  (X-Hoody-Signature header), identity claims issued at login, and container
  authorization claims.
  
  **Third-party verification flow:**
  1. Fetch this endpoint once (cache for 24h+)
  2. Locate the key by `kid` from the `keys[]` array
  3. For response signatures: parse `X-Hoody-Signature: t=<ts>,kid=<id>,m=<method>,s=<status>,path=<url>,sig=<hex>`
   and verify sig against the newline-joined tuple `${t}\n${method}\n${status}\n${path}\n${responseBody}`
  4. For identity/container claims: verify `claim.signature_hex` against `claim.payload_b64` bytes
  5. If `kid` in a signature/claim does not match any cached key, re-fetch this endpoint.
  
  No authentication required. This endpoint is intentionally public.
     * @param options._realm - Realm host-scope override (subdomain routing only)
     */
    getPublicKey(options?: {
        _realm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }): Promise<ApiMetaGetPublicKeyResponse>;
    /**
     * Get Hoody Social Counters
     *
     * Returns cached counters for the public Hoody social channels: GitHub stars,
  Telegram members, Discord members (total + currently online), X followers,
  and LinkedIn followers.
  
  Values are persisted to disk and refreshed in the background
  (default every 10 minutes), so the endpoint is cheap, has no upstream
  rate-limit risk, and survives process restarts even when upstreams are
  unreachable.
  
  A field is `null` only when no value has ever been persisted for it (e.g.
  first boot before the first successful refresh). Once populated it stays
  populated until manually cleared.
  
  No authentication required.
     * @param options._realm - Realm host-scope override (subdomain routing only)
     */
    getSocialStats(options?: {
        _realm?: string;
        signal?: AbortSignal;
        timeoutMs?: number;
        retries?: number;
        retryDelayMs?: number;
        retryOnStatuses?: number[];
        middlewareContext?: Record<string, unknown>;
        authRetry?: boolean;
        rawResponse?: boolean;
        responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob';
    }): Promise<GetSocialStatsResponse>;
}
