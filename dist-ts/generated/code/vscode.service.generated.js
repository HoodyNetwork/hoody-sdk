/**
 * VscodeServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in VscodeService.
 *
 * Generated at: 2026-07-16T14:47:21.000Z
 */
import { ValidationError } from '../errors.js';
export class VscodeServiceBase {
    defaultUrlTemplateVariables;
    http;
    _kitNamespace;
    urlTemplatePattern;
    constructor(http, namespace, defaultUrlTemplateVariables, urlTemplatePattern) {
        this.defaultUrlTemplateVariables = defaultUrlTemplateVariables;
        this._kitNamespace = namespace;
        this.http = namespace ? this._wrapHttpClient(http, namespace) : http;
        this.urlTemplatePattern = urlTemplatePattern;
    }
    /**
     * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
     */
    _wrapHttpClient(http, ns) {
        return new Proxy(http, {
            get: (target, prop) => {
                const val = target[prop];
                if (typeof val === 'function' && ['get', 'post', 'put', 'patch', 'delete', 'head', 'options', 'request'].includes(prop)) {
                    return (...args) => {
                        const dataIndex = prop === 'request' ? 2 : 1;
                        const data = args[dataIndex] ? { ...args[dataIndex] } : {};
                        data.middlewareContext = { ...data.middlewareContext, _kitNamespace: ns };
                        args[dataIndex] = data;
                        return val.apply(target, args);
                    };
                }
                return typeof val === 'function' ? val.bind(target) : val;
            }
        });
    }
    /**
     * Build URL with template variables or fallback to baseURL
     *
     * @param path - API endpoint path
     * @param variables - Method-level template variables to override defaults
     * @returns Full URL (template mode) or path only (baseURL mode)
     */
    buildTemplateUrl(path, variables) {
        // Get URL pattern: client override > default template pattern
        const urlPattern = this.urlTemplatePattern || 'https://{projectId}-{containerId}-code-{serviceIndex}.{server}.containers.hoody.com';
        // Merge variables: method level > client level
        const allVariables = { ...this.defaultUrlTemplateVariables, ...variables };
        const hasVariables = Object.keys(allVariables).length > 0;
        if (!hasVariables) {
            // BaseURL mode: No template variables provided
            // Return path only - HttpClient will prepend baseURL automatically
            return path;
        }
        // Template mode: Build full URL with variable substitution
        let url = urlPattern;
        for (const [key, value] of Object.entries(allVariables)) {
            if (value !== undefined) {
                url = url.replace(`{${key}}`, () => String(value));
            }
        }
        // Validate all variables were replaced (safety check)
        if (url.includes('{') && url.includes('}')) {
            const processRef = globalThis.process;
            if (processRef?.env?.SDK_DEBUG) {
                console.warn(`[Hoody SDK] URL template has unreplaced variables: ${url}`);
                console.warn(`[Hoody SDK] Falling back to baseURL mode.`);
            }
            // Fallback to baseURL mode if template incomplete
            return path;
        }
        return `${url}${path}`;
    }
    /**
     * Read nested values from objects using dotted paths.
     * Example: "data.items" or "data.pagination.total"
     */
    getPathValue(input, path) {
        if (!path)
            return undefined;
        const segments = path.split('.').filter(Boolean);
        let current = input;
        for (const segment of segments) {
            if (current === undefined || current === null) {
                return undefined;
            }
            if (Array.isArray(current) && segment === 'length') {
                current = current.length;
                continue;
            }
            if (typeof current !== 'object') {
                return undefined;
            }
            current = current[segment];
        }
        return current;
    }
    /**
     * Get VS Code web interface
     *
     * Returns the main VS Code web interface.
  
  ## Authentication
  - If authentication is enabled and user is not logged in, redirects to `/login`
  - Session is managed via cookies
  
  ## Workspace Handling
  - If no `folder` or `workspace` parameter: uses last opened or CLI argument
  - Stores query parameters in settings for next session
  - Supports both folder and workspace (.code-workspace) files
  
  ## Extension-Only Mode
  Add `?extension=PUBLISHER.NAME` to open in extension-only mode:
  - Hides file explorer
  - Focuses on extension's UI
  - Ideal for embedding extensions as standalone apps (like Cline)
  
  ## Custom Resources
  Use `--external-js` and `--external-css` flags to inject external resources:
  - JavaScript files are loaded in <head>
  - CSS files are linked as stylesheets
  - CSP headers are automatically updated to allow external domains
  
  ## Page Loader
  Enable `--page-loader` to show a branded loading overlay during initialization.
  The loader automatically dismisses when VS Code is ready.
     */
    async getVSCode(options, _templateVars) {
        const { folder, workspace, extension, ew, locale, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
        // Runtime constraint validation for folder
        if (folder !== undefined && folder !== null) {
        }
        // Runtime constraint validation for workspace
        if (workspace !== undefined && workspace !== null) {
        }
        // Runtime constraint validation for extension
        if (extension !== undefined && extension !== null) {
            if (typeof extension === 'string' && !new RegExp('^[a-zA-Z0-9-]+\\.[a-zA-Z0-9-]+$').test(extension)) {
                throw new ValidationError('extension must match pattern: ^[a-zA-Z0-9-]+\\.[a-zA-Z0-9-]+$', 'extension');
            }
        }
        // Runtime constraint validation for ew
        if (ew !== undefined && ew !== null) {
        }
        // Runtime constraint validation for locale
        if (locale !== undefined && locale !== null) {
            if (typeof locale === 'string' && !new RegExp('^[a-z]{2}(-[A-Z]{2})?$').test(locale)) {
                throw new ValidationError('locale must match pattern: ^[a-z]{2}(-[A-Z]{2})?$', 'locale');
            }
        }
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/code`, _templateVars || {});
        const requestData = {};
        // Build query params, filtering out undefined values (allows proxy auto-population)
        requestData.query = {};
        if (folder !== undefined) {
            requestData.query['folder'] = folder;
        }
        if (workspace !== undefined) {
            requestData.query['workspace'] = workspace;
        }
        if (extension !== undefined) {
            requestData.query['extension'] = extension;
        }
        if (ew !== undefined) {
            requestData.query['ew'] = ew;
        }
        if (locale !== undefined) {
            requestData.query['locale'] = locale;
        }
        if (signal) {
            requestData.signal = signal;
        }
        if (timeoutMs !== undefined) {
            requestData.timeoutMs = timeoutMs;
        }
        if (retries !== undefined) {
            requestData.retries = retries;
        }
        if (retryDelayMs !== undefined) {
            requestData.retryDelayMs = retryDelayMs;
        }
        if (retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = retryOnStatuses;
        }
        if (middlewareContext !== undefined) {
            requestData.middlewareContext = middlewareContext;
        }
        if (authRetry !== undefined) {
            requestData.authRetry = authRetry;
        }
        if (rawResponse !== undefined) {
            requestData.rawResponse = rawResponse;
        }
        if (responseType !== undefined) {
            requestData.responseType = responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
    /**
     * Get PWA manifest
     *
     * Returns the Progressive Web App manifest for installing Hoody Code.
  
  The manifest includes:
  - App name (configurable via --app-name)
  - Icons for various sizes
  - Display mode (fullscreen with window controls overlay)
  - Start URL
     */
    async getManifest(_templateVars, requestOptions) {
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/code/manifest.json`, _templateVars || {});
        const requestData = {};
        if (requestOptions?.signal) {
            requestData.signal = requestOptions.signal;
        }
        if (requestOptions?.timeoutMs !== undefined) {
            requestData.timeoutMs = requestOptions.timeoutMs;
        }
        if (requestOptions?.retries !== undefined) {
            requestData.retries = requestOptions.retries;
        }
        if (requestOptions?.retryDelayMs !== undefined) {
            requestData.retryDelayMs = requestOptions.retryDelayMs;
        }
        if (requestOptions?.retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = requestOptions.retryOnStatuses;
        }
        if (requestOptions?.middlewareContext !== undefined) {
            requestData.middlewareContext = requestOptions.middlewareContext;
        }
        if (requestOptions?.authRetry !== undefined) {
            requestData.authRetry = requestOptions.authRetry;
        }
        if (requestOptions?.rawResponse !== undefined) {
            requestData.rawResponse = requestOptions.rawResponse;
        }
        if (requestOptions?.responseType !== undefined) {
            requestData.responseType = requestOptions.responseType;
        }
        return this.http.get(requestUrl, requestData);
    }
    /**
     * Generate server web key
     *
     * Generates or retrieves the server's web key half used for encryption.
  
  The key is:
  - 256 bits (32 bytes)
  - Stored in user-data-dir/serve-web-key-half
  - Created once and reused across restarts
  - Used by VS Code for secure communications
     */
    async mintKey(_templateVars, requestOptions) {
        // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
        let requestUrl = this.buildTemplateUrl(`/api/v1/code/mint-key`, _templateVars || {});
        const requestData = {};
        if (requestOptions?.signal) {
            requestData.signal = requestOptions.signal;
        }
        if (requestOptions?.timeoutMs !== undefined) {
            requestData.timeoutMs = requestOptions.timeoutMs;
        }
        if (requestOptions?.retries !== undefined) {
            requestData.retries = requestOptions.retries;
        }
        if (requestOptions?.retryDelayMs !== undefined) {
            requestData.retryDelayMs = requestOptions.retryDelayMs;
        }
        if (requestOptions?.retryOnStatuses !== undefined) {
            requestData.retryOnStatuses = requestOptions.retryOnStatuses;
        }
        if (requestOptions?.middlewareContext !== undefined) {
            requestData.middlewareContext = requestOptions.middlewareContext;
        }
        if (requestOptions?.authRetry !== undefined) {
            requestData.authRetry = requestOptions.authRetry;
        }
        if (requestOptions?.rawResponse !== undefined) {
            requestData.rawResponse = requestOptions.rawResponse;
        }
        if (requestOptions?.responseType !== undefined) {
            requestData.responseType = requestOptions.responseType;
        }
        return this.http.post(requestUrl, requestData);
    }
}
