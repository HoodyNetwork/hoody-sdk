/**
 * IntrospectionControlServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in IntrospectionControlService.
 *
 * Generated at: 2026-07-27T22:22:39.000Z
 */

import { HttpClient } from '../http-client.js';
import { ValidationError } from '../errors.js';
import type {
  ApiResponse,
  BrowserIntrospectionGetMetadataResponse,
  BrowserMetadata,
  Error,
  BrowserIntrospectionListTabsResponse,
  Tab,
  BrowserIntrospectionCloseTabRequest,
  BrowserIntrospectionCloseTabResponse,
  BrowserIntrospectionShutdownResponse,
  BrowserIntrospectionGetDevtoolsUrlResponse,
  GetViewportResponse,
  ViewportStatus,
  SetViewportRequest,
  SetViewportResponse,
} from '../types.js';

export abstract class IntrospectionControlServiceBase {
  protected readonly http: HttpClient;
  protected readonly _kitNamespace: string | undefined;
  private readonly urlTemplatePattern: string | undefined;

  constructor(http: HttpClient, namespace?: string, private defaultUrlTemplateVariables?: Record<string, string | number>, urlTemplatePattern?: string) {
    this._kitNamespace = namespace;
    this.http = namespace ? this._wrapHttpClient(http, namespace) : http;
    this.urlTemplatePattern = urlTemplatePattern;
  }

  /**
   * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
   */
  private _wrapHttpClient(http: HttpClient, ns: string): HttpClient {
    return new Proxy(http, {
      get: (target, prop) => {
        const val = (target as any)[prop];
        if (typeof val === 'function' && ['get', 'post', 'put', 'patch', 'delete', 'head', 'options', 'request'].includes(prop as string)) {
          return (...args: any[]) => {
            const dataIndex = prop === 'request' ? 2 : 1;
            const data = args[dataIndex] ? { ...args[dataIndex] } : {};
            data.middlewareContext = { ...data.middlewareContext, _kitNamespace: ns };
            args[dataIndex] = data;
            return val.apply(target, args);
          };
        }
        return typeof val === 'function' ? val.bind(target) : val;
      }
    });
  }

  /**
   * Build URL with template variables or fallback to baseURL
   *
   * @param path - API endpoint path
   * @param variables - Method-level template variables to override defaults
   * @returns Full URL (template mode) or path only (baseURL mode)
   */
  protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string {
    // Get URL pattern: client override > default template pattern
    const urlPattern = this.urlTemplatePattern || 'https://{projectId}-{containerId}-browser-{serviceIndex}.{server}.containers.hoody.com';
    
    // Merge variables: method level > client level
    const allVariables = { ...this.defaultUrlTemplateVariables, ...variables };
    const hasVariables = Object.keys(allVariables).length > 0;
    
    if (!hasVariables) {
      // BaseURL mode: No template variables provided
      // Return path only - HttpClient will prepend baseURL automatically
      return path;
    }
    
    // Template mode: Build full URL with variable substitution
    let url = urlPattern;
    
    for (const [key, value] of Object.entries(allVariables)) {
      if (value !== undefined) {
        url = url.replace(`{${key}}`, () => String(value));
      }
    }
    
    // Validate all variables were replaced (safety check)
    if (url.includes('{') && url.includes('}')) {
      const processRef = (globalThis as { process?: { env?: Record<string, string | undefined> } }).process;
      if (processRef?.env?.SDK_DEBUG) {
        console.warn(`[Hoody SDK] URL template has unreplaced variables: ${url}`);
        console.warn(`[Hoody SDK] Falling back to baseURL mode.`);
      }
      // Fallback to baseURL mode if template incomplete
      return path;
    }
    
    return `${url}${path}`;
  }

  /**
   * Read nested values from objects using dotted paths.
   * Example: "data.items" or "data.pagination.total"
   */
  protected getPathValue(input: unknown, path: string): unknown {
    if (!path) return undefined;

    const segments = path.split('.').filter(Boolean);
    let current: unknown = input;

    for (const segment of segments) {
      if (current === undefined || current === null) {
        return undefined;
      }

      if (Array.isArray(current) && segment === 'length') {
        current = current.length;
        continue;
      }

      if (typeof current !== 'object') {
        return undefined;
      }

      current = (current as Record<string, unknown>)[segment];
    }

    return current;
  }

  /**
   * Get instance metadata
   *
   * Retrieves detailed metadata for an existing browser instance, including:
- Session information (ID, name)
- Browser details (name, version, user agent)
- Operating system information
- Viewport and geolocation settings
- Open tabs list
- **Chrome DevTools WebSocket URL** (when available)

The `webSocketDebuggerUrl` field provides the WebSocket endpoint for remote debugging.
Use this URL to connect Chrome DevTools, Puppeteer, or Playwright to the browser instance.
   */
  async getMetadata(options?: { browser_id?: string; start?: boolean; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<BrowserIntrospectionGetMetadataResponse> {
   const { browser_id, start, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Runtime constraint validation for browser_id
   if (browser_id !== undefined && browser_id !== null) {
   }
   // Runtime constraint validation for start
   if (start !== undefined && start !== null) {
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/metadata`, _templateVars || {});
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (browser_id !== undefined) {
      requestData.query['browser_id'] = browser_id;
    }
    if (start !== undefined) {
      requestData.query['start'] = start;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<BrowserIntrospectionGetMetadataResponse>(requestUrl, requestData);

  }


  /**
   * List browser tabs
   *
   * Lists all open tabs in a browser instance
   */
  async listTabs(options?: { browser_id?: string; start?: boolean; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<BrowserIntrospectionListTabsResponse> {
   const { browser_id, start, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Runtime constraint validation for browser_id
   if (browser_id !== undefined && browser_id !== null) {
   }
   // Runtime constraint validation for start
   if (start !== undefined && start !== null) {
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/tabs`, _templateVars || {});
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (browser_id !== undefined) {
      requestData.query['browser_id'] = browser_id;
    }
    if (start !== undefined) {
      requestData.query['start'] = start;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<BrowserIntrospectionListTabsResponse>(requestUrl, requestData);

  }


  /**
   * Close a browser tab
   *
   * Closes a specific browser tab by its tab ID. If no tabId is provided, closes the active tab (unless it is the last remaining tab).
   */
  async closeTab(data?: BrowserIntrospectionCloseTabRequest, options?: { browser_id?: string; start?: boolean; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<BrowserIntrospectionCloseTabResponse> {
   const { browser_id, start, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Runtime constraint validation for browser_id
   if (browser_id !== undefined && browser_id !== null) {
   }
   // Runtime constraint validation for start
   if (start !== undefined && start !== null) {
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/tab/close`, _templateVars || {});
    const requestData: any = {};
    requestData.body = data;
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (browser_id !== undefined) {
      requestData.query['browser_id'] = browser_id;
    }
    if (start !== undefined) {
      requestData.query['start'] = start;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.post<BrowserIntrospectionCloseTabResponse>(requestUrl, requestData);

  }


  /**
   * Shutdown browser instance
   *
   * Shuts down a specific browser instance
   */
  async shutdown(options?: { browser_id?: string; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<BrowserIntrospectionShutdownResponse> {
   const { browser_id, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Runtime constraint validation for browser_id
   if (browser_id !== undefined && browser_id !== null) {
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/shutdown`, _templateVars || {});
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (browser_id !== undefined) {
      requestData.query['browser_id'] = browser_id;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<BrowserIntrospectionShutdownResponse>(requestUrl, requestData);

  }


  /**
   * Get DevTools URLs
   *
   * Returns the Chrome DevTools WebSocket URL and HTTP discovery URL for the specified
browser instance. The HTTP URL (`/json/version`) can be used to resolve the WebSocket
endpoint automatically.

**Hoody CDP relay**: In container deployments these URLs are minted on the `cdp-{N}`
hostname (e.g. `https://{projectId}-{containerId}-cdp-{N}.{server}.containers.hoody.com`),
paired 1:1 with the `browser-{N}` instance that owns the debugging session. The relay is
reached HTTP-first: connect with `chromium.connectOverCDP("https://…-cdp-{N}/")`, which
fetches `/json/version` to resolve the live WebSocket endpoint before upgrading, and
cold-starts instance `N` on demand if it isn't already running. Only the read-only
discovery endpoints (`/json/version`, `/json/list`, `/json`) and DevTools WebSocket
sessions (`/devtools/browser/<id>`, `/devtools/page/<id>`) are relayed — mutation endpoints
(`/json/new`, `/json/close`, `/json/activate`) are not exposed.

**Availability**: These URLs are populated when the instance is launched with
`useRemoteDebuggingPort=true` and `browser=chromium`. Otherwise it will be `null`.
   */
  async getDevtoolsUrl(options?: { browser_id?: string; start?: boolean; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<BrowserIntrospectionGetDevtoolsUrlResponse> {
   const { browser_id, start, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Runtime constraint validation for browser_id
   if (browser_id !== undefined && browser_id !== null) {
   }
   // Runtime constraint validation for start
   if (start !== undefined && start !== null) {
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/devtools-url`, _templateVars || {});
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (browser_id !== undefined) {
      requestData.query['browser_id'] = browser_id;
    }
    if (start !== undefined) {
      requestData.query['start'] = start;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<BrowserIntrospectionGetDevtoolsUrlResponse>(requestUrl, requestData);

  }


  /**
   * Get the current viewport policy
   *
   * Read-only: returns the instance's current viewport policy without mutating anything.
`viewport: null` means fixed-viewport emulation is disabled (responsive — the page follows the real window size).
`source` is `creation` until the first successful `POST /viewport`, then `runtime`.
`converged` reports whether every live page currently reflects the policy.

**Never auto-creates an instance.** `browser_host`/`browser_port` may be omitted when exactly one instance is running (it is selected); with zero instances the response is 404, with more than one it is 400 `AMBIGUOUS_INSTANCE`.
   */
  async getViewport(options?: { browser_host?: string; browser_port?: number; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<GetViewportResponse> {
   const { browser_host, browser_port, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Runtime constraint validation for browser_host
   if (browser_host !== undefined && browser_host !== null) {
   }
   // Runtime constraint validation for browser_port
   if (browser_port !== undefined && browser_port !== null) {
     if (!Number.isFinite(browser_port) || !Number.isInteger(browser_port)) {
       throw new ValidationError('browser_port must be an integer', 'browser_port');
     }
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/viewport`, _templateVars || {});
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (browser_host !== undefined) {
      requestData.query['browser_host'] = browser_host;
    }
    if (browser_port !== undefined) {
      requestData.query['browser_port'] = browser_port;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<GetViewportResponse>(requestUrl, requestData);

  }


  /**
   * Change the viewport at runtime
   *
   * Mutates the running instance's viewport policy. JSON body only:
- Fixed size: `{"viewport": {"width": 1280, "height": 800}}` (integers 1..8192; no other keys)
- Responsive (follow the real window): `{"viewport": null}`

Applies to every currently-open tab; tabs opened later inherit the policy.

**Constraints**:
- Responsive requires a Chromium instance that was CREATED responsive (`noViewport=true`/`viewport=null` on /start). Instances created with a fixed viewport respond `409 REQUIRES_RESTART` (close and recreate the instance responsive instead).
- Firefox: fixed sizes work; responsive responds `501 NOT_SUPPORTED`.
- Partial application failures respond `502 VIEWPORT_APPLY_INCOMPLETE` with `details.failedTabs`.

**Never auto-creates an instance.** Instance selection rules are identical to `GET /viewport`.
   */
  async setViewport(data: SetViewportRequest, options?: { browser_host?: string; browser_port?: number; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<SetViewportResponse> {
   const { browser_host, browser_port, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Validate required parameter: data
   if (data === undefined || data === null) {
     throw new ValidationError('data is required', 'data');
   }
   // Runtime constraint validation for browser_host
   if (browser_host !== undefined && browser_host !== null) {
   }
   // Runtime constraint validation for browser_port
   if (browser_port !== undefined && browser_port !== null) {
     if (!Number.isFinite(browser_port) || !Number.isInteger(browser_port)) {
       throw new ValidationError('browser_port must be an integer', 'browser_port');
     }
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/viewport`, _templateVars || {});
    const requestData: any = {};
    requestData.body = data;
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (browser_host !== undefined) {
      requestData.query['browser_host'] = browser_host;
    }
    if (browser_port !== undefined) {
      requestData.query['browser_port'] = browser_port;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.post<SetViewportResponse>(requestUrl, requestData);

  }


}
