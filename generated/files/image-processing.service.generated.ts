/**
 * ImageProcessingServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in ImageProcessingService.
 *
 * Generated at: 2026-07-27T22:22:39.000Z
 */

import { HttpClient } from '../http-client.js';
import { ValidationError } from '../errors.js';
import type {
  ApiResponse,
} from '../types.js';

export abstract class ImageProcessingServiceBase {
  protected readonly http: HttpClient;
  protected readonly _kitNamespace: string | undefined;
  private readonly urlTemplatePattern: string | undefined;

  constructor(http: HttpClient, namespace?: string, private defaultUrlTemplateVariables?: Record<string, string | number>, urlTemplatePattern?: string) {
    this._kitNamespace = namespace;
    this.http = namespace ? this._wrapHttpClient(http, namespace) : http;
    this.urlTemplatePattern = urlTemplatePattern;
  }

  /**
   * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
   */
  private _wrapHttpClient(http: HttpClient, ns: string): HttpClient {
    return new Proxy(http, {
      get: (target, prop) => {
        const val = (target as any)[prop];
        if (typeof val === 'function' && ['get', 'post', 'put', 'patch', 'delete', 'head', 'options', 'request'].includes(prop as string)) {
          return (...args: any[]) => {
            const dataIndex = prop === 'request' ? 2 : 1;
            const data = args[dataIndex] ? { ...args[dataIndex] } : {};
            data.middlewareContext = { ...data.middlewareContext, _kitNamespace: ns };
            args[dataIndex] = data;
            return val.apply(target, args);
          };
        }
        return typeof val === 'function' ? val.bind(target) : val;
      }
    });
  }

  /**
   * Build URL with template variables or fallback to baseURL
   *
   * @param path - API endpoint path
   * @param variables - Method-level template variables to override defaults
   * @returns Full URL (template mode) or path only (baseURL mode)
   */
  protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string {
    // Get URL pattern: client override > default template pattern
    const urlPattern = this.urlTemplatePattern || '';
    
    // Merge variables: method level > client level
    const allVariables = { ...this.defaultUrlTemplateVariables, ...variables };
    const hasVariables = Object.keys(allVariables).length > 0;
    
    if (!hasVariables) {
      // BaseURL mode: No template variables provided
      // Return path only - HttpClient will prepend baseURL automatically
      return path;
    }
    
    // Template mode: Build full URL with variable substitution
    let url = urlPattern;
    
    for (const [key, value] of Object.entries(allVariables)) {
      if (value !== undefined) {
        url = url.replace(`{${key}}`, () => String(value));
      }
    }
    
    // Validate all variables were replaced (safety check)
    if (url.includes('{') && url.includes('}')) {
      const processRef = (globalThis as { process?: { env?: Record<string, string | undefined> } }).process;
      if (processRef?.env?.SDK_DEBUG) {
        console.warn(`[Hoody SDK] URL template has unreplaced variables: ${url}`);
        console.warn(`[Hoody SDK] Falling back to baseURL mode.`);
      }
      // Fallback to baseURL mode if template incomplete
      return path;
    }
    
    return `${url}${path}`;
  }

  /**
   * Read nested values from objects using dotted paths.
   * Example: "data.items" or "data.pagination.total"
   */
  protected getPathValue(input: unknown, path: string): unknown {
    if (!path) return undefined;

    const segments = path.split('.').filter(Boolean);
    let current: unknown = input;

    for (const segment of segments) {
      if (current === undefined || current === null) {
        return undefined;
      }

      if (Array.isArray(current) && segment === 'length') {
        current = current.length;
        continue;
      }

      if (typeof current !== 'object') {
        return undefined;
      }

      current = (current as Record<string, unknown>)[segment];
    }

    return current;
  }

  /**
   * Process and convert images
   *
   * On-the-fly image processing with format conversion, resizing, and effects. Supports JPEG, PNG, WebP, GIF, BMP input/output. Works for both local files and all 60+ remote cloud storage backends.
   */
  async process(image: string, options?: { thumbnail?: ""; format?: "jpeg" | "png" | "webp" | "gif" | "bmp"; size?: string; width?: number; height?: number; resize?: "fit" | "fill" | "cover" | "exact"; quality?: "low" | "medium" | "high"; q?: number; blur?: number; grayscale?: ""; bg?: string; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<ApiResponse<unknown>> {
   const { thumbnail, format, size, width, height, resize, quality, q, blur, grayscale, bg, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Validate required parameter: image
   if (image === undefined || image === null) {
     throw new ValidationError('image is required', 'image');
   }
   // Runtime constraint validation for image
   if (image !== undefined && image !== null) {
   }
   // Validate required parameter: thumbnail
   if (thumbnail === undefined || thumbnail === null) {
     throw new ValidationError('thumbnail is required', 'thumbnail');
   }
   // Runtime constraint validation for thumbnail
   if (thumbnail !== undefined && thumbnail !== null) {
     if (![''].includes(thumbnail)) {
       throw new ValidationError('thumbnail must be one of: ', 'thumbnail');
     }
   }
   // Runtime constraint validation for format
   if (format !== undefined && format !== null) {
     if (!['jpeg', 'png', 'webp', 'gif', 'bmp'].includes(format)) {
       throw new ValidationError('format must be one of: jpeg, png, webp, gif, bmp', 'format');
     }
   }
   // Runtime constraint validation for size
   if (size !== undefined && size !== null) {
     if (typeof size === 'string' && !new RegExp('^\\d+x\\d+$').test(size)) {
       throw new ValidationError('size must match pattern: ^\\d+x\\d+$', 'size');
     }
   }
   // Runtime constraint validation for width
   if (width !== undefined && width !== null) {
     if (!Number.isFinite(width) || !Number.isInteger(width)) {
       throw new ValidationError('width must be an integer', 'width');
     }
     if (width < 1) {
       throw new ValidationError('width must be >= 1', 'width');
     }
     if (width > 2000) {
       throw new ValidationError('width must be <= 2000', 'width');
     }
   }
   // Runtime constraint validation for height
   if (height !== undefined && height !== null) {
     if (!Number.isFinite(height) || !Number.isInteger(height)) {
       throw new ValidationError('height must be an integer', 'height');
     }
     if (height < 1) {
       throw new ValidationError('height must be >= 1', 'height');
     }
     if (height > 2000) {
       throw new ValidationError('height must be <= 2000', 'height');
     }
   }
   // Runtime constraint validation for resize
   if (resize !== undefined && resize !== null) {
     if (!['fit', 'fill', 'cover', 'exact'].includes(resize)) {
       throw new ValidationError('resize must be one of: fit, fill, cover, exact', 'resize');
     }
   }
   // Runtime constraint validation for quality
   if (quality !== undefined && quality !== null) {
     if (!['low', 'medium', 'high'].includes(quality)) {
       throw new ValidationError('quality must be one of: low, medium, high', 'quality');
     }
   }
   // Runtime constraint validation for q
   if (q !== undefined && q !== null) {
     if (!Number.isFinite(q) || !Number.isInteger(q)) {
       throw new ValidationError('q must be an integer', 'q');
     }
     if (q < 1) {
       throw new ValidationError('q must be >= 1', 'q');
     }
     if (q > 100) {
       throw new ValidationError('q must be <= 100', 'q');
     }
   }
   // Runtime constraint validation for blur
   if (blur !== undefined && blur !== null) {
     if (!Number.isFinite(blur)) {
       throw new ValidationError('blur must be a finite number', 'blur');
     }
     if (blur > 50) {
       throw new ValidationError('blur must be <= 50', 'blur');
     }
   }
   // Runtime constraint validation for grayscale
   if (grayscale !== undefined && grayscale !== null) {
     if (![''].includes(grayscale)) {
       throw new ValidationError('grayscale must be one of: ', 'grayscale');
     }
   }
   // Runtime constraint validation for bg
   if (bg !== undefined && bg !== null) {
     if (typeof bg === 'string' && !new RegExp('^#?[0-9a-fA-F]{6}$').test(bg)) {
       throw new ValidationError('bg must match pattern: ^#?[0-9a-fA-F]{6}$', 'bg');
     }
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/{image}?thumbnail`, _templateVars || {});
    // Replace path parameters
    requestUrl = requestUrl.replace('{' + 'image' + '}', () => encodeURIComponent(String(image)));
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (thumbnail !== undefined) {
      requestData.query['thumbnail'] = thumbnail;
    }
    if (format !== undefined) {
      requestData.query['format'] = format;
    }
    if (size !== undefined) {
      requestData.query['size'] = size;
    }
    if (width !== undefined) {
      requestData.query['width'] = width;
    }
    if (height !== undefined) {
      requestData.query['height'] = height;
    }
    if (resize !== undefined) {
      requestData.query['resize'] = resize;
    }
    if (quality !== undefined) {
      requestData.query['quality'] = quality;
    }
    if (q !== undefined) {
      requestData.query['q'] = q;
    }
    if (blur !== undefined) {
      requestData.query['blur'] = blur;
    }
    if (grayscale !== undefined) {
      requestData.query['grayscale'] = grayscale;
    }
    if (bg !== undefined) {
      requestData.query['bg'] = bg;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<ApiResponse<unknown>>(requestUrl, requestData);

  }


}
