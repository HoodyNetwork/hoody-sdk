/**
 * CurlServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in CurlService.
 *
 * Generated at: 2026-07-26T20:08:12.000Z
 */

import { HttpClient } from '../http-client.js';
import { ValidationError } from '../errors.js';
import type {
  ApiResponse,
  ExecuteCurlRequestGetResponse,
  JsonResponse,
  CurlExecuteRequest,
  CurlExecuteResponse,
  CurlRequest,
} from '../types.js';

export abstract class CurlServiceBase {
  protected readonly http: HttpClient;
  protected readonly _kitNamespace: string | undefined;
  private readonly urlTemplatePattern: string | undefined;

  constructor(http: HttpClient, namespace?: string, private defaultUrlTemplateVariables?: Record<string, string | number>, urlTemplatePattern?: string) {
    this._kitNamespace = namespace;
    this.http = namespace ? this._wrapHttpClient(http, namespace) : http;
    this.urlTemplatePattern = urlTemplatePattern;
  }

  /**
   * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
   */
  private _wrapHttpClient(http: HttpClient, ns: string): HttpClient {
    return new Proxy(http, {
      get: (target, prop) => {
        const val = (target as any)[prop];
        if (typeof val === 'function' && ['get', 'post', 'put', 'patch', 'delete', 'head', 'options', 'request'].includes(prop as string)) {
          return (...args: any[]) => {
            const dataIndex = prop === 'request' ? 2 : 1;
            const data = args[dataIndex] ? { ...args[dataIndex] } : {};
            data.middlewareContext = { ...data.middlewareContext, _kitNamespace: ns };
            args[dataIndex] = data;
            return val.apply(target, args);
          };
        }
        return typeof val === 'function' ? val.bind(target) : val;
      }
    });
  }

  /**
   * Build URL with template variables or fallback to baseURL
   *
   * @param path - API endpoint path
   * @param variables - Method-level template variables to override defaults
   * @returns Full URL (template mode) or path only (baseURL mode)
   */
  protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string {
    // Get URL pattern: client override > default template pattern
    const urlPattern = this.urlTemplatePattern || 'https://{projectId}-{containerId}-curl-{serviceIndex}.{serverName}.containers.hoody.com';
    
    // Merge variables: method level > client level
    const allVariables = { ...this.defaultUrlTemplateVariables, ...variables };
    const hasVariables = Object.keys(allVariables).length > 0;
    
    if (!hasVariables) {
      // BaseURL mode: No template variables provided
      // Return path only - HttpClient will prepend baseURL automatically
      return path;
    }
    
    // Template mode: Build full URL with variable substitution
    let url = urlPattern;
    
    for (const [key, value] of Object.entries(allVariables)) {
      if (value !== undefined) {
        url = url.replace(`{${key}}`, () => String(value));
      }
    }
    
    // Validate all variables were replaced (safety check)
    if (url.includes('{') && url.includes('}')) {
      const processRef = (globalThis as { process?: { env?: Record<string, string | undefined> } }).process;
      if (processRef?.env?.SDK_DEBUG) {
        console.warn(`[Hoody SDK] URL template has unreplaced variables: ${url}`);
        console.warn(`[Hoody SDK] Falling back to baseURL mode.`);
      }
      // Fallback to baseURL mode if template incomplete
      return path;
    }
    
    return `${url}${path}`;
  }

  /**
   * Read nested values from objects using dotted paths.
   * Example: "data.items" or "data.pagination.total"
   */
  protected getPathValue(input: unknown, path: string): unknown {
    if (!path) return undefined;

    const segments = path.split('.').filter(Boolean);
    let current: unknown = input;

    for (const segment of segments) {
      if (current === undefined || current === null) {
        return undefined;
      }

      if (Array.isArray(current) && segment === 'length') {
        current = current.length;
        continue;
      }

      if (typeof current !== 'object') {
        return undefined;
      }

      current = (current as Record<string, unknown>)[segment];
    }

    return current;
  }

  /**
   * Execute simple HTTP request via query parameters
   *
   * Simplified interface for executing HTTP requests using URL query parameters. Best suited for simple GET requests and quick testing. For advanced features use POST endpoint.
   */
  async executeCurlRequestGet(options?: { url?: string; method?: string; response?: string; mode?: string; session_id?: string; follow_redirects?: boolean; timeout?: number; user_agent?: string; referer?: string; bearer_token?: string; save?: boolean; save_path?: string; insecure?: boolean; compressed?: boolean; job_name?: string; data?: string; json?: string; header?: string[]; data_base64?: string; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<ExecuteCurlRequestGetResponse> {
   const { url, method, response, mode, session_id, follow_redirects, timeout, user_agent, referer, bearer_token, save, save_path, insecure, compressed, job_name, data, json, header, data_base64, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Validate required parameter: url
   if (url === undefined || url === null) {
     throw new ValidationError('url is required', 'url');
   }
   // Runtime constraint validation for url
   if (url !== undefined && url !== null) {
   }
   // Runtime constraint validation for method
   if (method !== undefined && method !== null) {
   }
   // Runtime constraint validation for response
   if (response !== undefined && response !== null) {
   }
   // Runtime constraint validation for mode
   if (mode !== undefined && mode !== null) {
   }
   // Runtime constraint validation for session_id
   if (session_id !== undefined && session_id !== null) {
   }
   // Runtime constraint validation for follow_redirects
   if (follow_redirects !== undefined && follow_redirects !== null) {
   }
   // Runtime constraint validation for timeout
   if (timeout !== undefined && timeout !== null) {
     if (!Number.isFinite(timeout) || !Number.isInteger(timeout)) {
       throw new ValidationError('timeout must be an integer', 'timeout');
     }
   }
   // Runtime constraint validation for user_agent
   if (user_agent !== undefined && user_agent !== null) {
   }
   // Runtime constraint validation for referer
   if (referer !== undefined && referer !== null) {
   }
   // Runtime constraint validation for bearer_token
   if (bearer_token !== undefined && bearer_token !== null) {
   }
   // Runtime constraint validation for save
   if (save !== undefined && save !== null) {
   }
   // Runtime constraint validation for save_path
   if (save_path !== undefined && save_path !== null) {
   }
   // Runtime constraint validation for insecure
   if (insecure !== undefined && insecure !== null) {
   }
   // Runtime constraint validation for compressed
   if (compressed !== undefined && compressed !== null) {
   }
   // Runtime constraint validation for job_name
   if (job_name !== undefined && job_name !== null) {
   }
   // Runtime constraint validation for data
   if (data !== undefined && data !== null) {
   }
   // Runtime constraint validation for json
   if (json !== undefined && json !== null) {
   }
   // Runtime constraint validation for header
   if (header !== undefined && header !== null) {
   }
   // Runtime constraint validation for data_base64
   if (data_base64 !== undefined && data_base64 !== null) {
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/api/v1/curl/request`, _templateVars || {});
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (url !== undefined) {
      requestData.query['url'] = url;
    }
    if (method !== undefined) {
      requestData.query['method'] = method;
    }
    if (response !== undefined) {
      requestData.query['response'] = response;
    }
    if (mode !== undefined) {
      requestData.query['mode'] = mode;
    }
    if (session_id !== undefined) {
      requestData.query['session_id'] = session_id;
    }
    if (follow_redirects !== undefined) {
      requestData.query['follow_redirects'] = follow_redirects;
    }
    if (timeout !== undefined) {
      requestData.query['timeout'] = timeout;
    }
    if (user_agent !== undefined) {
      requestData.query['user_agent'] = user_agent;
    }
    if (referer !== undefined) {
      requestData.query['referer'] = referer;
    }
    if (bearer_token !== undefined) {
      requestData.query['bearer_token'] = bearer_token;
    }
    if (save !== undefined) {
      requestData.query['save'] = save;
    }
    if (save_path !== undefined) {
      requestData.query['save_path'] = save_path;
    }
    if (insecure !== undefined) {
      requestData.query['insecure'] = insecure;
    }
    if (compressed !== undefined) {
      requestData.query['compressed'] = compressed;
    }
    if (job_name !== undefined) {
      requestData.query['job_name'] = job_name;
    }
    if (data !== undefined) {
      requestData.query['data'] = data;
    }
    if (json !== undefined) {
      requestData.query['json'] = json;
    }
    if (header !== undefined) {
      requestData.query['header'] = header;
    }
    if (data_base64 !== undefined) {
      requestData.query['data_base64'] = data_base64;
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<ExecuteCurlRequestGetResponse>(requestUrl, requestData);

  }


  /**
   * Execute HTTP request with full cURL capabilities
   *
   * Execute an HTTP request using libcurl with comprehensive configuration options. Supports both synchronous (immediate response) and asynchronous (background job) execution modes. Includes advanced features like retry logic, cookie sessions, proxy configuration, and automatic response storage.

**Execution Modes:**
- `sync` (default): Blocks until completion, returns immediate response
- `async`: Returns job ID immediately, executes in background

**Response Modes:**
- `transparent` (default): Returns raw response with original headers
- `json`: Wraps response in JSON with timing metrics and metadata

**Example Use Cases:**
- API integration with automatic retry
- Large file downloads with progress tracking
- Multi-step authentication flows with session cookies
- Scheduled recurring requests with cron expressions
   */
  async execute(data: CurlExecuteRequest, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }, requestOptions?: { signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }): Promise<CurlExecuteResponse> {
   // Validate required parameter: data
   if (data === undefined || data === null) {
     throw new ValidationError('data is required', 'data');
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/api/v1/curl/request`, _templateVars || {});
    const requestData: any = {};
    requestData.body = data;
    if (requestOptions?.signal) {
      requestData.signal = requestOptions.signal;
    }
    if (requestOptions?.timeoutMs !== undefined) {
      requestData.timeoutMs = requestOptions.timeoutMs;
    }
    if (requestOptions?.retries !== undefined) {
      requestData.retries = requestOptions.retries;
    }
    if (requestOptions?.retryDelayMs !== undefined) {
      requestData.retryDelayMs = requestOptions.retryDelayMs;
    }
    if (requestOptions?.retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = requestOptions.retryOnStatuses;
    }
    if (requestOptions?.middlewareContext !== undefined) {
      requestData.middlewareContext = requestOptions.middlewareContext;
    }
    if (requestOptions?.authRetry !== undefined) {
      requestData.authRetry = requestOptions.authRetry;
    }
    if (requestOptions?.rawResponse !== undefined) {
      requestData.rawResponse = requestOptions.rawResponse;
    }
    if (requestOptions?.responseType !== undefined) {
      requestData.responseType = requestOptions.responseType;
    }
    return this.http.post<CurlExecuteResponse>(requestUrl, requestData);

  }


}
