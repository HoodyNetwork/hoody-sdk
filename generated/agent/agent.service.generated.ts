/**
 * AgentServiceBase
 * Generated from OpenAPI specification
 *
 * ⚠️ DO NOT EDIT THIS FILE MANUALLY ⚠️
 * This file is automatically generated and will be overwritten.
 * To add custom methods, extend this class in AgentService.
 *
 * Generated at: 2026-08-05T22:37:04.000Z
 */

import { HttpClient } from '../http-client.js';
import { ValidationError } from '../errors.js';
import type {
  ApiResponse,
} from '../types.js';

export abstract class AgentServiceBase {
  protected readonly http: HttpClient;
  protected readonly _kitNamespace: string | undefined;
  private readonly urlTemplatePattern: string | undefined;

  constructor(http: HttpClient, namespace?: string, private defaultUrlTemplateVariables?: Record<string, string | number>, urlTemplatePattern?: string) {
    this._kitNamespace = namespace;
    this.http = namespace ? this._wrapHttpClient(http, namespace) : http;
    this.urlTemplatePattern = urlTemplatePattern;
  }

  /**
   * Wrap HttpClient to auto-inject _kitNamespace into middlewareContext
   */
  private _wrapHttpClient(http: HttpClient, ns: string): HttpClient {
    return new Proxy(http, {
      get: (target, prop) => {
        const val = (target as any)[prop];
        if (typeof val === 'function' && ['get', 'post', 'put', 'patch', 'delete', 'head', 'options', 'request'].includes(prop as string)) {
          return (...args: any[]) => {
            const dataIndex = prop === 'request' ? 2 : 1;
            const data = args[dataIndex] ? { ...args[dataIndex] } : {};
            data.middlewareContext = { ...data.middlewareContext, _kitNamespace: ns };
            args[dataIndex] = data;
            return val.apply(target, args);
          };
        }
        return typeof val === 'function' ? val.bind(target) : val;
      }
    });
  }

  /**
   * Build URL with template variables or fallback to baseURL
   *
   * @param path - API endpoint path
   * @param variables - Method-level template variables to override defaults
   * @returns Full URL (template mode) or path only (baseURL mode)
   */
  protected buildTemplateUrl(path: string, variables: Record<string, string | number | undefined>): string {
    // Get URL pattern: client override > default template pattern
    const urlPattern = this.urlTemplatePattern || 'https://{host}/api/v1/agent';
    
    // Merge variables: method level > client level
    const allVariables = { ...this.defaultUrlTemplateVariables, ...variables };
    const hasVariables = Object.keys(allVariables).length > 0;
    
    if (!hasVariables) {
      // BaseURL mode: No template variables provided
      // Return path only - HttpClient will prepend baseURL automatically
      return path;
    }
    
    // Template mode: Build full URL with variable substitution
    let url = urlPattern;
    
    for (const [key, value] of Object.entries(allVariables)) {
      if (value !== undefined) {
        url = url.replace(`{${key}}`, () => String(value));
      }
    }
    
    // Validate all variables were replaced (safety check)
    if (url.includes('{') && url.includes('}')) {
      const processRef = (globalThis as { process?: { env?: Record<string, string | undefined> } }).process;
      if (processRef?.env?.SDK_DEBUG) {
        console.warn(`[Hoody SDK] URL template has unreplaced variables: ${url}`);
        console.warn(`[Hoody SDK] Falling back to baseURL mode.`);
      }
      // Fallback to baseURL mode if template incomplete
      return path;
    }
    
    return `${url}${path}`;
  }

  /**
   * Read nested values from objects using dotted paths.
   * Example: "data.items" or "data.pagination.total"
   */
  protected getPathValue(input: unknown, path: string): unknown {
    if (!path) return undefined;

    const segments = path.split('.').filter(Boolean);
    let current: unknown = input;

    for (const segment of segments) {
      if (current === undefined || current === null) {
        return undefined;
      }

      if (Array.isArray(current) && segment === 'length') {
        current = current.length;
        continue;
      }

      if (typeof current !== 'object') {
        return undefined;
      }

      current = (current as Record<string, unknown>)[segment];
    }

    return current;
  }

  /**
   * Export logs as a downloadable file.
   *
   * Streams the filtered, ALWAYS-redacted log set as a download (Content-Disposition: attachment). format=jsonl (default, one wire row per line) or txt (human-readable). Local sources export a point-in-time-consistent snapshot of the in-daemon ring (bounded at the seq observed when the export starts); source=activity|events|proxy exports ONE platform query page (default limit 2000) and marks the export partial when more remained. Every export ends with an in-band terminator (jsonl: a final {"_hoody_export":{...}} line; txt: a trailing '# export:...' comment) carrying rows/gap/partial — its absence means the download was truncated. Filters mirror the Logs tab exactly (source, min_level, comp, session_id, text, since, until, event, tool, model, status, method, min_status, max_status, errors_only, event_type, resource_type, container_id, kind, host); ?since_seq exports incrementally; ?limit caps total rows; ?filename overrides the download name (reduced to a safe basename). Active-only: a realm header returns 400 realm_scope_unsupported.
   */
  async exportLogs(options?: { source?: string; min_level?: string; comp?: string; session_id?: string; text?: string; since?: string; until?: string; event?: string; tool?: string; model?: string; status?: string; method?: string; min_status?: number; max_status?: number; errors_only?: boolean; event_type?: string; resource_type?: string; container?: string; kind?: string; host?: string; since_seq?: number; limit?: number; format?: string; filename?: string; realm?: string; XHoodyCwd?: string; XHoodyConfigDir?: string; XHoodyContainer?: string; XHoodyRealm?: string; signal?: AbortSignal; timeoutMs?: number; retries?: number; retryDelayMs?: number; retryOnStatuses?: number[]; middlewareContext?: Record<string, unknown>; authRetry?: boolean; rawResponse?: boolean; responseType?: 'auto' | 'json' | 'text' | 'arrayBuffer' | 'blob'; }, _templateVars?: { projectId?: string; containerId?: string; serviceIndex?: string | number; server?: string }): Promise<ApiResponse<unknown>> {
   const { source, min_level, comp, session_id, text, since, until, event, tool, model, status, method, min_status, max_status, errors_only, event_type, resource_type, container, kind, host, since_seq, limit, format, filename, realm, XHoodyCwd, XHoodyConfigDir, XHoodyContainer, XHoodyRealm, signal, timeoutMs, retries, retryDelayMs, retryOnStatuses, middlewareContext, authRetry, rawResponse, responseType } = options || {};
   // Runtime constraint validation for source
   if (source !== undefined && source !== null) {
   }
   // Runtime constraint validation for min_level
   if (min_level !== undefined && min_level !== null) {
   }
   // Runtime constraint validation for comp
   if (comp !== undefined && comp !== null) {
   }
   // Runtime constraint validation for session_id
   if (session_id !== undefined && session_id !== null) {
   }
   // Runtime constraint validation for text
   if (text !== undefined && text !== null) {
   }
   // Runtime constraint validation for since
   if (since !== undefined && since !== null) {
   }
   // Runtime constraint validation for until
   if (until !== undefined && until !== null) {
   }
   // Runtime constraint validation for event
   if (event !== undefined && event !== null) {
   }
   // Runtime constraint validation for tool
   if (tool !== undefined && tool !== null) {
   }
   // Runtime constraint validation for model
   if (model !== undefined && model !== null) {
   }
   // Runtime constraint validation for status
   if (status !== undefined && status !== null) {
   }
   // Runtime constraint validation for method
   if (method !== undefined && method !== null) {
   }
   // Runtime constraint validation for min_status
   if (min_status !== undefined && min_status !== null) {
     if (!Number.isFinite(min_status) || !Number.isInteger(min_status)) {
       throw new ValidationError('min_status must be an integer', 'min_status');
     }
   }
   // Runtime constraint validation for max_status
   if (max_status !== undefined && max_status !== null) {
     if (!Number.isFinite(max_status) || !Number.isInteger(max_status)) {
       throw new ValidationError('max_status must be an integer', 'max_status');
     }
   }
   // Runtime constraint validation for errors_only
   if (errors_only !== undefined && errors_only !== null) {
   }
   // Runtime constraint validation for event_type
   if (event_type !== undefined && event_type !== null) {
   }
   // Runtime constraint validation for resource_type
   if (resource_type !== undefined && resource_type !== null) {
   }
   // Runtime constraint validation for container
   if (container !== undefined && container !== null) {
   }
   // Runtime constraint validation for kind
   if (kind !== undefined && kind !== null) {
   }
   // Runtime constraint validation for host
   if (host !== undefined && host !== null) {
   }
   // Runtime constraint validation for since_seq
   if (since_seq !== undefined && since_seq !== null) {
     if (!Number.isFinite(since_seq) || !Number.isInteger(since_seq)) {
       throw new ValidationError('since_seq must be an integer', 'since_seq');
     }
   }
   // Runtime constraint validation for limit
   if (limit !== undefined && limit !== null) {
     if (!Number.isFinite(limit) || !Number.isInteger(limit)) {
       throw new ValidationError('limit must be an integer', 'limit');
     }
   }
   // Runtime constraint validation for format
   if (format !== undefined && format !== null) {
   }
   // Runtime constraint validation for filename
   if (filename !== undefined && filename !== null) {
   }
   // Runtime constraint validation for realm
   if (realm !== undefined && realm !== null) {
   }
   // Runtime constraint validation for XHoodyCwd
   if (XHoodyCwd !== undefined && XHoodyCwd !== null) {
   }
   // Runtime constraint validation for XHoodyConfigDir
   if (XHoodyConfigDir !== undefined && XHoodyConfigDir !== null) {
   }
   // Runtime constraint validation for XHoodyContainer
   if (XHoodyContainer !== undefined && XHoodyContainer !== null) {
   }
   // Runtime constraint validation for XHoodyRealm
   if (XHoodyRealm !== undefined && XHoodyRealm !== null) {
   }

    // Build URL with template variables (from constructor's defaultUrlTemplateVariables or method-level _templateVars)
    let requestUrl = this.buildTemplateUrl(`/api/v1/agent/logs/export`, _templateVars || {});
    const requestData: any = {};
    // Build query params, filtering out undefined values (allows proxy auto-population)
    requestData.query = {};
    if (source !== undefined) {
      requestData.query['source'] = source;
    }
    if (min_level !== undefined) {
      requestData.query['min_level'] = min_level;
    }
    if (comp !== undefined) {
      requestData.query['comp'] = comp;
    }
    if (session_id !== undefined) {
      requestData.query['session_id'] = session_id;
    }
    if (text !== undefined) {
      requestData.query['text'] = text;
    }
    if (since !== undefined) {
      requestData.query['since'] = since;
    }
    if (until !== undefined) {
      requestData.query['until'] = until;
    }
    if (event !== undefined) {
      requestData.query['event'] = event;
    }
    if (tool !== undefined) {
      requestData.query['tool'] = tool;
    }
    if (model !== undefined) {
      requestData.query['model'] = model;
    }
    if (status !== undefined) {
      requestData.query['status'] = status;
    }
    if (method !== undefined) {
      requestData.query['method'] = method;
    }
    if (min_status !== undefined) {
      requestData.query['min_status'] = min_status;
    }
    if (max_status !== undefined) {
      requestData.query['max_status'] = max_status;
    }
    if (errors_only !== undefined) {
      requestData.query['errors_only'] = errors_only;
    }
    if (event_type !== undefined) {
      requestData.query['event_type'] = event_type;
    }
    if (resource_type !== undefined) {
      requestData.query['resource_type'] = resource_type;
    }
    if (container !== undefined) {
      requestData.query['container'] = container;
    }
    if (kind !== undefined) {
      requestData.query['kind'] = kind;
    }
    if (host !== undefined) {
      requestData.query['host'] = host;
    }
    if (since_seq !== undefined) {
      requestData.query['since_seq'] = since_seq;
    }
    if (limit !== undefined) {
      requestData.query['limit'] = limit;
    }
    if (format !== undefined) {
      requestData.query['format'] = format;
    }
    if (filename !== undefined) {
      requestData.query['filename'] = filename;
    }
    if (realm !== undefined) {
      requestData.query['realm'] = realm;
    }
    // Forward declared header parameters (e.g. If-Match) into requestData.headers.
    requestData.headers = requestData.headers || {};
    if (XHoodyCwd !== undefined) {
      requestData.headers['X-Hoody-Cwd'] = String(XHoodyCwd);
    }
    if (XHoodyConfigDir !== undefined) {
      requestData.headers['X-Hoody-Config-Dir'] = String(XHoodyConfigDir);
    }
    if (XHoodyContainer !== undefined) {
      requestData.headers['X-Hoody-Container'] = String(XHoodyContainer);
    }
    if (XHoodyRealm !== undefined) {
      requestData.headers['X-Hoody-Realm'] = String(XHoodyRealm);
    }
    if (signal) {
      requestData.signal = signal;
    }
    if (timeoutMs !== undefined) {
      requestData.timeoutMs = timeoutMs;
    }
    if (retries !== undefined) {
      requestData.retries = retries;
    }
    if (retryDelayMs !== undefined) {
      requestData.retryDelayMs = retryDelayMs;
    }
    if (retryOnStatuses !== undefined) {
      requestData.retryOnStatuses = retryOnStatuses;
    }
    if (middlewareContext !== undefined) {
      requestData.middlewareContext = middlewareContext;
    }
    if (authRetry !== undefined) {
      requestData.authRetry = authRetry;
    }
    if (rawResponse !== undefined) {
      requestData.rawResponse = rawResponse;
    }
    if (responseType !== undefined) {
      requestData.responseType = responseType;
    }
    return this.http.get<ApiResponse<unknown>>(requestUrl, requestData);

  }


}
